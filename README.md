# WWW Project — Phase 0–12

**What We Wonder／大家想知道** — privacy-preserving public poll platform.

This repository implements delivery milestones **Phase 0 through Phase 12**: Poll Core, Reference Answer Design B, Official Vote, Result Display, frontend privacy closure (5A), the public freshness-only discovery feed (5B–5C), poll visibility/archive (6A), **admin typo correction governance (6B/6C)**, **admin correction audit read + review-context hardening (7.3B–7.5)**, the poll-scoped **public notice read API (8)**, the safe global **admin correction audit queue (9)**, DB-backed integration coverage synchronization for the Phase 8/9 read surfaces (10), the poll-scoped **public notice display UI (11)**, and the server-side **Admin Auth / RBAC v1 boundary (12)**.

Normative rules: `/AGENTS.md` and `docs/www-project-agent-spec-v0.1.md`.

Milestone summaries: `docs/www-project-milestone-phase-0-5b-handoff-v1.md` (through 5B), `docs/www-project-milestone-phase-0-5c-handoff-v1.md` (5C feed pagination), `docs/www-project-milestone-phase-10-handoff-v1.md` (Phase 8/9 PG integration synchronization), `docs/www-project-milestone-phase-11-handoff-v1.md` (public notice display UI), and `docs/www-project-milestone-phase-12-handoff-v1.md` (Admin Auth / RBAC v1).

**Phase 14 (docs):** Admin Auth v1 production deployment handoff — `docs/www-project-phase-14-admin-auth-deployment-v1.md` (configure and verify `ADMIN_AUTH_CREDENTIALS_JSON` before admin UI / JWT / OAuth work).

**Phase 15 (docs):** PostgreSQL integration test setup — `docs/www-project-phase-15-pg-integration-test-setup-v1.md` (use `npm run test:integration:local` for the isolated local Docker `www_test`, or run `npm run test:integration` when `DATABASE_URL` is set explicitly).

**Phase 20 (docs):** Correction / admin governance handoff index (Phase 8–19 baseline, validation commands, privacy boundaries, TODOs) — `docs/www-project-phase-20-correction-admin-governance-handoff-v1.md`.

**Phase 27 (docs):** Public MVP manual QA handoff (local setup, validation commands, browser checklist for `/` → create → vote → results) — `docs/www-project-public-mvp-manual-qa-v1.md`. **Lifecycle / creator live flow:** prefer Phase 60 doc first when testing `?live=1` and `?creator=1`.

**Phase 31 (docs):** Public MVP demo/release handoff (showcase script, validation checklist, privacy boundaries, out-of-scope list) — `docs/www-project-public-mvp-demo-release-handoff-v1.md`.

**Phase 32 (docs):** Local public MVP demo startup (Docker `www_test`, session `DATABASE_URL`, migrate, `npm start`, troubleshooting) — `docs/www-project-local-demo-startup-v1.md`.

**Phase 37:** Local public MVP demo helper — `npm run demo:public:local` (seeds fake official demo voters on `www_test`, starts server); CSP fix so `public-mvp.css` loads. See `docs/www-project-local-demo-startup-v1.md`.

**Phase 33 (docs):** Public MVP manual QA checklist pass — doc cross-links and smoke-aligned route checks; see `docs/www-project-public-mvp-manual-qa-v1.md` (read order: startup → demo handoff → manual QA).

**Phase 34 (docs):** Cross-browser / device manual QA result log template — `docs/www-project-public-mvp-cross-browser-qa-log-v1.md` (fill when testing real browsers; smoke does not replace this).

**Phase 36 (docs):** Cross-browser QA log baseline updated — `docs/www-project-public-mvp-cross-browser-qa-log-v1.md` (limited Chromium automation + BLOCKED/SKIP for Chrome/Edge/Safari/mobile; Windows Chrome/Edge still need human fill-in).

**Phase 35 (docs):** Production readiness boundary — `docs/www-project-production-readiness-boundary-v1.md` (public MVP is demo-ready, not production-ready; gates and checklist before external trial).

**Phase 39 (docs, policy only — not implemented):** Poll lifecycle — close, reveal, public lock period, cancel, unpublish/archive, collecting result visibility — `docs/www-project-phase-39-poll-lifecycle-policy-v1.md`. Read together with Phase 40.

**Phase 40 (docs, policy only — not implemented):** User profile, voting eligibility (age/region), ineligible UX, follow-result notification — `docs/www-project-phase-40-user-profile-eligibility-follow-policy-v1.md`. Implementation must satisfy **both** Phase 39 and Phase 40 (no collecting-stage result leakage; founders without intermediate signals during collecting).

**Phase 41 (docs, planning only — not implemented):** Public MVP UI policy implementation plan — maps Phase 39 lifecycle and Phase 40 eligibility/follow policies onto current pages (`/`, `/polls/new`, `/vote/:id`, `/results/:id`, `/explore`); classifies UI-only vs schema/API work — `docs/www-project-phase-41-public-mvp-ui-policy-implementation-plan-v1.md`.

**Phase 47–48 (Public MVP static UI):** FAQ page (`/faq`), trust-level permission matrix (`/trust-levels`), policy-aligned demo copy, mobile readability polish — commit **`630baea`** on `origin/master` after Phase 48 push. Browser surfaces are **static/demo-facing**; normative drafts remain `docs/www-project-public-faq-draft-v1.md` and `docs/www-project-trust-level-policy-draft-v1.md`. Align with Phase 39–41.

**Phase 49 (docs):** Public MVP demo/status sync — this README section and `docs/www-project-public-mvp-demo-release-handoff-v1.md` (demo routes, product rules, not-yet-implemented list).

**Phase 50:** Public MVP demo QA and copy consistency — commit **`023cf9b`** (`fix: polish public mvp demo consistency`).

**Phase 51 (docs, planning only — not implemented):** Real MVP implementation boundary — baseline `023cf9b`, risk categories, product invariants, suggested Phase 52–60 roadmap from planning through lifecycle backend (no DB/auth/notification/scoring in this phase) — `docs/www-project-phase-51-real-mvp-implementation-boundary-v1.md`.

**Phase 52 (docs, planning only — not implemented):** Real MVP data model plan — entities, lifecycle states, privacy-sensitive fields, Phase 54 migration prep (no schema/API in this phase) — `docs/www-project-phase-52-real-mvp-data-model-plan-v1.md`. Baseline planning chain: Phase 51 @ `eab9a91`.

**Phase 53 (docs, planning only — not implemented):** Public poll lifecycle API/spec plan — endpoints, state gates, collecting privacy responses, error model (no route implementation in this phase) — `docs/www-project-phase-53-public-lifecycle-api-spec-plan-v1.md`. Baseline @ `6d71358`.

**Phase 54:** Real MVP public lifecycle schema foundation — `migrations/008_phase54_public_lifecycle_foundation.sql` adds server-controlled `polls.public_lifecycle_state`, reveal/lock/cancel/unpublish timestamps, scheduler-support indexes, and minimal `poll_eligibility_rules`. No public lifecycle routes, frontend behavior, auth/session logic, result snapshots, or counter exposure were added. Decisions and rollback notes: `docs/www-project-phase-54-public-lifecycle-schema-foundation-v1.md`.

**Phase 55A:** Public result lifecycle guard — existing `GET /polls/:id/results` reads aggregate counters only when `public_lifecycle_state` is `revealed`, `locked`, or `post_lock`. Collecting and unavailable states return a counter-free shell; legacy `status='closed'` and vote-count thresholds do not reveal results by themselves. No new route, schema, frontend, auth/session, scheduler, or notification behavior was added.

**Phase 55B:** Public read response alignment — existing `GET /polls/:id/results` responses now expose `public_lifecycle_state`; unavailable shells include lifecycle-safe copy without aggregate reads. Existing `GET /polls/:id` detail responses add the same safe lifecycle field. This remains a response-only alignment before public create/read expansion: no new route, schema, frontend behavior, auth/session, scheduler, or notification behavior was added.

**Phase 55C:** Public result page lifecycle rendering — `public/frontend/result-page.js` consumes `public_lifecycle_state` and `user_message` from existing read APIs; lifecycle collecting and unavailable shells stay counter-free, while revealed/locked/post_lock render display-safe aggregates without treating display-tier sub-30 `collecting` as lifecycle collecting. Demo `ui_state` previews unchanged. No backend schema, route, auth, or notification changes.

**Phase 55D (docs only):** Lifecycle transition API boundary plan — documents server-controlled `collecting → cancelled`, `collecting → revealed`, `revealed → locked`, `locked → post_lock`, and `post_lock → unpublished` boundaries before implementation. Phase 57 remains the proposed implementation phase; Phase 56 stays reserved for eligibility and collecting privacy guardrails. See `docs/www-project-phase-55d-lifecycle-transition-api-boundary-plan-v1.md`.

**Phase 56:** Eligibility and collecting privacy guardrails — public collecting results remain one identity-neutral counter-free shell for guests, creators, currently ineligible users, and eligible users. Shared participation checks now require `public_lifecycle_state='collecting'`, preventing Official Vote and Reference Answer after cancel/reveal/lock/post-lock/unpublish. At Phase 56, the full age/region `poll_eligibility_rules` evaluator remained future because user profile fields were not yet implemented; Phase 66C has since implemented the Official Vote profile eligibility evaluator only. See `docs/www-project-phase-56-eligibility-collecting-privacy-guardrails-v1.md`.

**Phase 57:** Public lifecycle transition write APIs — creator-authenticated `POST /polls/:id/cancel`, `POST /polls/:id/close`, and `POST /polls/:id/unpublish` now use bounded row-lock transactions with server-written timestamps. Internal `revealPoll` and `advancePublicLifecycle` service helpers support scheduler calls without deploying a scheduler. `public_lifecycle_state` remains authoritative; collecting and unavailable result paths remain counter-free. See `docs/www-project-phase-57-lifecycle-transition-write-apis-v1.md`.

**Phase 58A:** Lifecycle scheduler advancement foundation — `createPublicLifecycleSchedulerService` can advance `revealed → locked` and `locked → post_lock` in bounded batches; **no cron or deployment wiring yet**. See `docs/www-project-phase-58a-lifecycle-scheduler-advancement-foundation-v1.md`.

**Phase 58B–58D:** Frontend creator lifecycle controls on `/polls/new?live=1`, `/my-polls?live=1`, and `/results/:id?creator=1`; result display re-fetch after successful transitions; safe copy when re-fetch fails. See `docs/www-project-phase-58b-frontend-creator-lifecycle-controls-v1.md`.

**Phase 59:** Public MVP creator flow polish — shared Traditional Chinese guidance for vote sharing, poll management links, and cancel / close-reveal / unpublish semantics. See `docs/www-project-phase-59-public-mvp-creator-flow-polish-v1.md`.

**Phase 60 (docs):** Public MVP lifecycle manual QA and handoff refresh (Phases 57–59 live flow, checklist, known limitations) — `docs/www-project-phase-60-public-mvp-lifecycle-manual-qa-handoff-v1.md`. Updates read order in `docs/www-project-public-mvp-manual-qa-v1.md`.

**Phase 60F–61 (docs):** README Public MVP status — live creator routes (`?live=1`, `?creator=1`), Minimal public flow, and `GET /polls/:id/results` lifecycle gating wording.

**Phase 62 (docs):** Public MVP status checkpoint and next-phase planning (post Phases 57–61; candidate phases with risk labels) — `docs/www-project-phase-62-public-mvp-status-checkpoint-v1.md`.

**Phase 63:** Public explore feed UI — `GET /explore` lists collecting polls from existing `GET /polls/feed` (freshness-only; no vote counts, ranking, or personalization).

**Phase 64:** Explicit lifecycle scheduler runner — after build, `npm run scheduler:lifecycle -- --limit 100` runs one bounded server-side batch for due `revealed → locked` and `locked → post_lock` transitions. It is safe for manual or cron invocation, but no production cron is installed by this repository. See `docs/www-project-phase-64-lifecycle-scheduler-runner-v1.md`.

**Phase 65A:** Creator session foundation — digest-backed fixed-TTL `creator_session` cookies, exact-match Origin gate, local/test-only issuance, and production fail-closed behavior. No frontend UX or creator ownership rewiring. See `docs/www-project-phase-65a-creator-session-foundation-v1.md`.

**Phase 65B:** Backend creator-owned poll APIs — authenticated `/creator/polls` create/list/delete/lifecycle routes use only the Phase 65A cookie principal. Owned list reads are bounded, deterministic, polls-table-only, and counter-free. The then-deferred legacy `/polls` creator-write risk is resolved by Phase 65C-A/B. See `docs/www-project-phase-65b-creator-owned-poll-apis-v1.md`.

**Phase 65C-A:** Live creator frontend cutover — `/polls/new?live=1`, `/my-polls?live=1`, and creator lifecycle controls now use Phase 65B `/creator/polls` APIs with the Phase 65A cookie. Local/demo live mode bootstraps only the local-test creator session; no production login UX, schema change, ranking, scheduler, or legacy route retirement was added in that phase. The remaining legacy `/polls` creator-write risk is resolved by Phase 65C-B. See `docs/www-project-phase-65c-a-frontend-creator-api-cutover-v1.md`.

**Phase 65C-B:** Legacy creator-write retirement — development-era `/polls` creator writes that trusted client-selected `X-User-Id` now fail closed with `410 LEGACY_CREATOR_WRITE_RETIRED`. There is no dev or production bypass. Use `/creator/polls` with the Phase 65A creator cookie for create/delete/lifecycle writes. Public reads, vote, vote-by-index, Reference Answer, feed, notices, results, and scheduler behavior are unchanged. See `docs/www-project-phase-65c-b-legacy-creator-write-retirement-v1.md`.

**Phase 65 (docs):** Final creator auth & ownership checkpoint — summarizes 65A session, 65B `/creator/polls`, 65C-A live frontend cutover, 65C-B legacy 410 retirement, remaining non-goals, and Phase 66 plan-only entry — `docs/www-project-phase-65-final-creator-auth-ownership-checkpoint-v1.md`.

**Phase 66A (docs):** Profile / eligibility / demographic qualification boundary spec (66P-R approved; 66B schema limited to display name, birth year-month, region; gender excluded; vote-by-index eligibility ordering and indistinguishable denials) — `docs/www-project-phase-66-profile-eligibility-boundary-spec-v1.md`.

**Phase 66B:** Minimal user profile schema foundation — `migrations/010_phase66b_user_profile_foundation.sql` adds nullable user-scoped `birth_year_month` and `residential_region`; existing `users.display_name` remains the display field. No gender, exact birthday, evaluator, API/runtime/frontend behavior, vote flow, Reference Answer behavior, ranking, personalization, or demographic breakdowns were added.

**Phase 66C:** Profile eligibility evaluator only — Official Vote and `vote-by-index` now check `poll_eligibility_rules` against nullable user profile fields inside the vote transaction before option resolution, vote token write, or counter increment. Reference Answer eligibility, frontend UX, ranking, personalization, demographic breakdowns, public result behavior, and creator eligibility edits remain unchanged.

**Phase 66E-P (docs):** Profile update API foundation plan — user-authenticated future API draft for `birth_year_month` and coarse `residential_region` only; no runtime, schema, frontend, Reference Answer, ranking, result, feed, notice, scheduler, or demographic behavior changes — `docs/www-project-phase-66e-profile-update-api-plan-v1.md`.

**Phase 66E-A:** Profile update API foundation — `GET /users/me/profile` and `PUT /users/me/profile` read/update only `birth_year_month` and coarse `residential_region` through user auth. No gender, exact birthday, migration, frontend UX, Reference Answer eligibility, ranking, result, feed, notice, scheduler, personalization, demographic breakdown, vote-time snapshot, backfill, or historical recalculation was added.

**Phase 66F-P (docs):** Profile UX / onboarding plan — future profile UI may collect only `birth_year_month` and coarse `residential_region` for Official Vote eligibility prompts; no gender, exact birthday, precise location, runtime/frontend/API/schema, Reference Answer, ranking, personalization, demographic, snapshot, backfill, or recalculation behavior changes — `docs/www-project-phase-66f-profile-ux-onboarding-plan-v1.md`.

**Phase 66F:** Profile UX — `GET /profile` collects only `birth_year_month` and coarse `residential_region` via `GET`/`PUT /users/me/profile` with MVP `X-User-Id` (no creator cookie). Save/clear, fixed ineligible vote copy, mobile-readable form. No gender, exact birthday, precise location, Reference Answer eligibility, ranking, or demographic breakdowns.

**Phase 66 (docs):** Final profile eligibility checkpoint — summarizes 66A–66F (boundary spec, schema, evaluator, route tests, profile API, profile UX), vote-by-index ordering, Reference Answer exclusion, Raw Option Linkage Ban, manual QA checklist, and MVP `X-User-Id` auth limitation — `docs/www-project-phase-66-final-profile-eligibility-checkpoint-v1.md`.

**Phase 67 (docs + UX copy):** Profile eligibility demo QA & public UX hardening — end-to-end demo/QA handoff for `/profile`, live creator flow, vote eligibility copy, results boundaries; README index; fixed vote policy panel + copy guard tests. Frontend auth remains MVP `X-User-Id` (production auth later). No schema, evaluator, transaction order, or Reference Answer scope changes — `docs/www-project-phase-67-profile-eligibility-demo-qa-v1.md`.

**Phase 68 (docs + UX copy):** Public demo polish & manual QA closure — README demo test order, integrated Phase 65–67 manual QA (creator session, profile, vote eligibility, results), FAQ/handoff updates, public page copy consistency, copy guard tests. No schema, auth, evaluator, or Reference Answer scope changes — `docs/www-project-phase-68-public-demo-polish-manual-qa-closure-v1.md`.

**Phase 69 (docs):** MVP demo release readiness & handoff closure — consolidates Phases 65–68 into a showcase-ready, testable, handoff state: release readiness checklist, demo startup, tester operation order, known limits (MVP `X-User-Id`, production auth later, scheduler not in `npm start`), and invariant boundaries. **Recommended tester entry:** `docs/www-project-phase-69-mvp-demo-release-readiness-handoff-v1.md`. No schema, auth, evaluator, or Reference Answer scope changes.

**Phase 70A:** Production user auth resolver foundation — `src/auth/user-auth-resolver.ts` defines `UserAuthResolver` / `AuthenticatedUserContext`, production fail-closed behavior without a trusted verifier, and explicit local/test MVP `X-User-Id` compatibility marked as non-production. `/users/me/profile`, Official Vote, `vote-by-index`, creator-owned routes, and Reference Answer are not cut over yet; Phase 70B is the profile API cutover. Plan and boundaries: `docs/www-project-phase-70-production-user-auth-account-boundary-plan-v1.md`.

**Phase 70B:** Profile API production auth cutover — `GET /users/me/profile` and `PUT /users/me/profile` now resolve identity via `UserAuthResolver` instead of reading raw `X-User-Id` directly. Production (`APP_ENV=production`) fail-closed unless a trusted credential verifier is configured; local/demo/test (`APP_ENV=development|test`) keep explicit MVP `X-User-Id` compatibility. `creator_session` does not authorize profile routes. Vote, vote-by-index, creator, and Reference Answer routes are unchanged. No schema, response shape, or eligibility evaluator changes.

**Phase 70C-P (docs):** Official Vote UserAuth cutover plan — specifies how future `POST /polls/:id/vote` and `POST /polls/:id/vote-by-index` should move to `UserAuthResolver` while preserving transaction order, `vote-by-index` ineligible indistinguishability, Reference Answer separation, creator-session separation, and the Raw Option Linkage Ban. No runtime, migration, API behavior, vote transaction, Reference Answer, result/feed/notices/scheduler/ranking/personalization, or creator behavior changes — `docs/www-project-phase-70c-official-vote-user-auth-cutover-plan-v1.md`.

**Phase 70C-I:** Official Vote route adapter UserAuth cutover — `POST /polls/:id/vote` and `POST /polls/:id/vote-by-index` now resolve identity via `UserAuthResolver` instead of reading raw `X-User-Id` directly. Production (`APP_ENV=production`) fail-closed unless a trusted credential verifier is configured; local/demo/test keep explicit MVP `X-User-Id` compatibility. `creator_session` does not authorize public vote routes. Reference Answer, profile API, service transaction order, token/counter schema, and eligibility evaluator are unchanged. Plan: `docs/www-project-phase-70c-official-vote-user-auth-cutover-plan-v1.md`.

**Phase 70D-P (docs):** Production creator_session strategy plan — recommends keeping `creator_session` local/demo-only while production `/creator/*` moves toward formal `UserAuthResolver` user auth plus creator ownership checks. This is specification only: no runtime, migration, API behavior, profile, vote, Reference Answer, ranking, result, feed, notice, scheduler, or frontend behavior has been implemented — `docs/www-project-phase-70d-production-creator-session-strategy-plan-v1.md`.

**Phase 70D-I-A (docs):** Creator route adapter UserAuth cutover plan — specifies the future production `/creator/*` route adapter move from creator_session-only authority to `UserAuthResolver` plus creator ownership checks, with route-by-route rules and validation requirements. This is specification only; no runtime, migration, API behavior, creator session, profile, vote, Reference Answer, ranking, result, feed, notice, scheduler, or frontend behavior has been implemented — `docs/www-project-phase-70d-creator-route-adapter-userauth-cutover-plan-v1.md`.

**Phase 70D-I-B:** Creator route adapter UserAuth cutover — production `/creator/polls` and production `GET /creator/session` now resolve creator identity via `UserAuthResolver` instead of `creator_session` or raw `X-User-Id`. Production (`APP_ENV=production`) fail-closed unless a trusted credential verifier is configured; `POST /creator/session` issuance remains fail-closed. Local/demo/test keep explicit `creator_session` compatibility (non-production identity). `creator_session` does not authorize public vote, vote-by-index, profile, or Reference Answer. Service ownership checks, Origin gate, and owned-list counter-free behavior are unchanged. Plan: `docs/www-project-phase-70d-creator-route-adapter-userauth-cutover-plan-v1.md`.

**Phase 70 (final checkpoint):** Production auth boundary cutover checkpoint — consolidates Phase 70 plan, 70A resolver foundation, 70B profile cutover, 70C Official Vote / vote-by-index cutover, and 70D creator_session strategy + `/creator/*` cutover. Production route adapters for profile, vote, vote-by-index, and creator-owned polls use `UserAuthResolver`; raw `X-User-Id` is not production identity; `creator_session` remains local/demo/test-only and does not authorize public vote, profile, or Reference Answer. Reference Answer is not cut over to `UserAuthResolver`. Remaining work: production credential verifier, formal user session integration, and frontend production login UX — `docs/www-project-phase-70-final-production-auth-boundary-checkpoint-v1.md`.

**Phase 71 (docs):** Production credential verifier / formal user session planning — defines the future `trustedCredentialVerifier` responsibility boundary, formal session credential lifecycle, frontend production login UX minimum boundary, explicit non-production-only `X-User-Id` compatibility, `creator_session` exclusion from production public identity, Reference Answer deferral, fail-closed rollback, and auth/privacy guardrails. No runtime, frontend, migration, schema, API behavior, vote transaction, profile eligibility, Reference Answer, creator route, ranking, personalization, logging, metrics, APM, debug, analytics, or error payload behavior changes — `docs/www-project-phase-71-production-credential-verifier-plan-v1.md`.

**Phase 72:** Production credential verifier foundation — adds a minimal `USER_AUTH_CREDENTIALS_JSON` opaque Bearer verifier for `UserAuthResolver` production wiring. Config stores SHA-256 token digests, canonical `user_id`, `expires_at`, and optional `revoked_at`; missing verifier config remains production fail-closed. Local/demo/test `X-User-Id` compatibility remains explicit non-production only; `creator_session` is still not production public identity. No login UX, frontend behavior, migration, DB schema, API behavior, vote token/counter schema, vote transaction order, profile eligibility, Reference Answer, creator route behavior, ranking, personalization, analytics, metrics, traces, debug payload, or error payload behavior changes — `docs/www-project-phase-72-production-credential-verifier-foundation-v1.md`.

**Phase 73 (docs):** Production login / session UX planning — defines the future production login UX minimum flow, logout / expired / revoked credential copy boundaries, frontend credential storage risks, production vs local/demo/test auth flow split, `Authorization: Bearer` + `UserAuthResolver` boundary, `creator_session` exclusion from production identity, Reference Answer deferral, fail-closed rollback, and auth/privacy guardrails. No runtime, frontend, migration, DB schema, API behavior, verifier behavior, vote transaction, profile eligibility, vote token/counter schema, Reference Answer, creator route behavior, ranking, personalization, analytics linkage, precise location, extra profile field, logging, metrics, APM, trace, debug payload, or error payload behavior changes — `docs/www-project-phase-73-production-login-session-ux-plan-v1.md`.

**Phase 74:** Production login UI shell / auth state UX foundation — adds static `GET /login` page and `login-page.js` shell explaining that **正式登入尚未啟用**, production protected routes **fail closed** without an approved verifier, and local/demo continues explicit MVP `X-User-Id` / `creator_session` test identity. Guest header links route to `/login`; form controls are disabled and do not call login APIs or browser credential storage. No DB, session issuance, `UserAuthResolver`/verifier runtime change, or protected API behavior change — `docs/www-project-phase-74-production-login-ui-shell-checkpoint-v1.md`.

**Phase 75:** Auth state display / navigation UX polish — shared `auth-state-copy.js`, header auth state chips, `renderAuthStateBanner`, and clearer demo nav switch copy across home, profile, vote, and my-polls pages. Distinguishes production login-not-yet-enabled, MVP `X-User-Id` test identity, and `creator_session` creator-only demo flow; guest CTAs stay aligned with `/login`. No DB, session issuance, verifier runtime change, or protected API behavior change — `docs/www-project-phase-75-auth-state-navigation-ux-checkpoint-v1.md`.

**Phase 76 (docs):** Public demo release checkpoint / auth UX QA closure — consolidates Phase 70–75 auth/login/demo UX state, demo testable routes (`/`、`/login`、`/profile`、`?live=1` creator flow、`/vote/:id`、`/results/:id`、`/explore`), production `UserAuthResolver` + Bearer verifier foundation vs disabled `/login` shell vs explicit non-production `X-User-Id` / `creator_session`, Reference Answer deferral, and sealed invariants (Official Vote transaction order, vote-by-index eligibility before option resolve, vote token / counter schema, Raw Option Linkage Ban). No runtime, frontend UX, DB, API, or verifier behavior change — `docs/www-project-phase-76-public-demo-auth-ux-qa-closure-checkpoint-v1.md`.

**Phase 77 (docs):** Production login session runtime plan — compares HttpOnly Secure SameSite cookie sessions with `Authorization: Bearer` opaque credentials, recommends cookie session as the next production browser login/session runtime, and defines future `UserAuthResolver` / `trustedCredentialVerifier`, login submit, logout, expiration, revocation, CSRF/XSS, non-production `X-User-Id`, `creator_session`, Reference Answer, migration/DB schema, and auth/privacy boundaries. No runtime, frontend, DB, migration, API behavior, verifier behavior, vote transaction, vote token/counter schema, Reference Answer, ranking, personalization, demographic, analytics, logging, metrics, APM, trace, debug payload, or error payload behavior change — `docs/www-project-phase-77-production-login-session-runtime-plan-v1.md`.

**Phase 78:** Production session schema foundation — `migrations/011_phase78_production_user_session_foundation.sql` adds digest-backed `user_sessions` lifecycle storage (`session_id`, `token_sha256`, `user_id`, timestamps) plus repository and tests. No login submit, `Set-Cookie`, formal logout API, `UserAuthResolver` behavior, production `trustedCredentialVerifier` behavior, protected API behavior, vote transaction, profile eligibility, vote token/counter schema, Reference Answer, ranking, personalization, demographic, analytics, logging, metrics, APM, trace, debug payload, or error payload behavior changed — `docs/www-project-phase-78-production-session-schema-foundation-v1.md`.

**Phase 79 (docs):** Production login submit / session cookie issuance plan — defines future login submit, raw session token handling, digest-only `token_sha256` persistence, `Set-Cookie` flags, expiration, revocation, `last_used_at`, logout/revoke, CSRF, `UserAuthResolver` / `trustedCredentialVerifier`, production vs local/demo/test identity, `creator_session`, `X-User-Id`, Reference Answer, vote, Raw Option Linkage Ban, and privacy boundaries. No runtime, route, migration, schema, cookie issuance, login submit, logout API, CSRF runtime, verifier, resolver, protected API, frontend, vote, profile, Reference Answer, ranking, analytics, logging, metrics, APM, trace, debug payload, or error payload behavior change — `docs/www-project-phase-79-production-login-session-cookie-plan-v1.md`.

**Phase 80:** Production login submit runtime foundation — `POST /login/session` is available only when backed by the existing `trustedCredentialVerifier` boundary and Phase 78 `user_sessions`; successful verification issues a Secure HttpOnly SameSite=Lax `www_session` cookie while persisting only `token_sha256`. `DELETE /login/session` revokes the current valid session by digest and clears the cookie. Protected APIs, frontend login UI submit behavior, Official Vote, `vote-by-index`, profile eligibility, Reference Answer, creator-session identity, ranking, analytics, logs/metrics/APM/error payloads, vote token schema, and counter schema are unchanged — `docs/www-project-phase-80-production-login-submit-runtime-foundation-v1.md`.

**Phase 81:** UserAuthResolver session read integration — when configured with the Phase 78 repository, `UserAuthResolver` can resolve production identity from `www_session` by hashing the raw cookie token and looking up `user_sessions.token_sha256`; missing, malformed, unknown, expired, revoked, and inactive-user sessions fail closed, and `last_used_at` updates only after successful verification. Non-production `X-User-Id` compatibility is unchanged; `creator_session` remains separate local/demo/test creator flow only. Official Vote, `vote-by-index`, Reference Answer, profile eligibility, frontend login UI, ranking, analytics, vote token schema, and counter schema are unchanged — `docs/www-project-phase-81-user-auth-resolver-session-read-integration-v1.md`.

**Phase 82:** Authenticated me endpoint foundation — `GET /users/me` resolves identity through `UserAuthResolver` and returns only `user_id` and `display_name`. Production identity follows Phase 81 `www_session` behavior; development/test preserve explicit `X-User-Id` compatibility. No session internals, token/cookie values, profile eligibility fields, vote or option data, frontend login UI, Reference Answer, Official Vote, ranking, analytics, vote token schema, or counter schema changes — `docs/www-project-phase-82-authenticated-me-endpoint-foundation-v1.md`.

**Phase 83:** Frontend login state read / minimal UI hook — site chrome calls `GET /users/me` with `credentials: 'same-origin'` and shows only `display_name` in the header when authenticated; logged-out state stays quiet. No login/registration form UI, logout button, backend auth/session behavior change, profile update UI change, or protected API behavior change — `docs/www-project-phase-83-frontend-login-state-read-v1.md`.

**Phase 84:** Frontend logout UI hook — when Phase 83 read shows a signed-in `display_name`, a small **登出** control calls `DELETE /login/session` with `credentials: 'same-origin'`; success hides the signed-in display and restores guest/demo auth chips, failure shows a neutral message only. No login/registration form UI, backend auth/session behavior change, `GET /users/me` response shape change, or protected API behavior change — `docs/www-project-phase-84-frontend-logout-ui-hook-v1.md`.

**Phase 85 (docs):** Production login UI plan — defines the future minimal production login form UX that will call existing `POST /login/session`, refresh Phase 83 state with `GET /users/me`, preserve Phase 84 logout behavior, and keep production session identity separate from local/demo/test `X-User-Id` and `creator_session`. No runtime, frontend form implementation, backend auth/session behavior, `UserAuthResolver`, `GET /users/me` response shape, Official Vote, Reference Answer, profile eligibility, ranking, analytics, vote token schema, or counter schema changes — `docs/www-project-phase-85-production-login-ui-plan-v1.md`.

**Phase 86:** Minimal production login form runtime — `/login` now offers a single credential-proof form that calls existing `POST /login/session` with `credentials: 'same-origin'`, refreshes Phase 83 `GET /users/me` state on `201`, and preserves Phase 84 logout behavior. Failure copy is neutral; no backend auth/session behavior, `UserAuthResolver`, `GET /users/me` shape, Official Vote, Reference Answer, profile eligibility, ranking, analytics, vote token schema, or counter schema changes — `docs/www-project-phase-86-minimal-production-login-form-runtime-v1.md`.

**Phase 87:** Production login runtime review / hardening — reviewed Phase 78-86 `user_sessions`, `POST`/`DELETE /login/session`, `UserAuthResolver` `www_session` verification, `GET /users/me`, frontend login-state read/logout UI, and `/login` form. No runtime defect requiring schema, migration, API, Official Vote, `vote-by-index`, Reference Answer, profile eligibility, ranking, logging, metrics, APM, trace, debug payload, analytics, or error payload changes was found; documentation now records the auth/privacy boundary confirmation — `docs/www-project-phase-87-production-login-runtime-review-hardening-v1.md`.

**Phase 88 (docs):** Registration / profile setup plan — defines a future production account setup flow requiring `display_name`, `birth_year_month`, and coarse `residential_region`, while excluding gender, exact birthday, precise location, registration runtime, profile setup UI, schema changes, login/session behavior changes, Official Vote transaction changes, Reference Answer integration, ranking personalization, demographic breakdowns, and analytics linkage — `docs/www-project-phase-88-registration-profile-setup-plan-v1.md`.

**Phase 89:** Registration runtime foundation — adds verifier-backed `POST /registration` to prepare a production user with `display_name`, month-granular `birth_year_month`, and coarse `residential_region`. It does not issue `www_session`; successful responses return `{ registered: true, login_required: true }` and the browser must still use existing `POST /login/session` for session issuance. No schema, `/users/me`, login/session, Official Vote, `vote-by-index`, Reference Answer, ranking, analytics, gender, exact birthday, precise location, or extra profile-field behavior changes — `docs/www-project-phase-89-registration-runtime-foundation-v1.md`.

**Phase 90:** Registration runtime review / hardening — reviewed Phase 89 `POST /registration` parsing, `display_name`, `birth_year_month`, coarse `residential_region`, verifier-only `Authorization: Bearer` credential proof handling, duplicate/conflict behavior, repository writes, response/error bodies, docs, and tests. No runtime defect requiring schema, migration, login/session, `UserAuthResolver`, `/users/me`, Official Vote, `vote-by-index`, Reference Answer, ranking, analytics, logs, metrics, APM, trace, debug payload, or error payload changes was found; focused tests now explicitly cover already-rejected sensitive profile/session/token/cookie/option fields — `docs/www-project-phase-90-registration-runtime-review-hardening-v1.md`.

**Phase 91 (docs):** Minimal registration UI plan — defines a future production registration form that calls existing `POST /registration` with `display_name`, month-granular `birth_year_month`, coarse `residential_region`, and `Authorization: Bearer <proof>`; explains why eligibility fields are required, maps safe validation/error copy, and requires registration success to proceed to existing `/login` without session or `Set-Cookie`. No registration UI runtime, backend API behavior, login/session, `UserAuthResolver`, `/users/me` shape, gender, exact birthday, precise location, or extra profile-field changes — `docs/www-project-phase-91-minimal-registration-ui-plan-v1.md`.

**Phase 92:** Minimal registration form runtime — adds `GET /registration` and `registration-page.js` to call existing `POST /registration` with `display_name`, month-granular `birth_year_month`, coarse `residential_region`, and credential proof sent only as `Authorization: Bearer <proof>`. Success guides users to `/login` without treating them as logged in, calling `/users/me`, calling `POST /login/session`, or reading `Set-Cookie`. No backend registration behavior, login/session, `UserAuthResolver`, `/users/me` shape, schema, vote, Reference Answer, ranking, analytics, gender, exact birthday, precise location, or extra profile-field changes — `docs/www-project-phase-92-minimal-registration-form-runtime-v1.md`.

**Phase 93:** Registration UI runtime review / hardening — reviewed `GET /registration`, `registration-page.js`, form fields, submit behavior, success/failure copy, credential proof transport, and mobile/accessibility basics. A minimal page-level guard now disables shared login-state reads on `/registration`, preventing `/users/me` calls from registration chrome while preserving normal login-state behavior elsewhere. No backend registration behavior, login/session, `UserAuthResolver`, `/users/me` shape, schema, vote, Reference Answer, ranking, analytics, gender, exact birthday, precise location, or extra profile-field changes — `docs/www-project-phase-93-registration-ui-runtime-review-hardening-v1.md`.

**Phase 94:** Registration/login navigation & copy polish — clarifies that registration creates account data only, does not log users in, and should be followed by `/login`, while login controls signed-in header state. Updates homepage, header auth chips, auth banner, `/login`, and `/registration` links/copy only. No backend auth/session, `POST /registration`, `UserAuthResolver`, `/users/me` shape, registration login-state-read opt-out removal, or schema changes — `docs/www-project-phase-94-registration-login-navigation-copy-polish-v1.md`.

**Phase 95:** Registration/login full flow smoke coverage — adds frontend unit tests, HTTP flow tests, static route assertions, and `smoke:public:local` checks for guest auth navigation, registration login-state-read opt-out, registration-to-login-only success behavior, login `GET /users/me` refresh with `display_name` only, and logout guest/demo chip restoration. No backend auth/session, `POST /registration`, `UserAuthResolver`, `/users/me` shape, registration login-state-read opt-out removal, or schema changes — `docs/www-project-phase-95-registration-login-full-flow-smoke-coverage-v1.md`.

**Phase 96:** Registration runtime/profile boundary review — reviews Phase 89–95 registration/login/profile flow, confirms registration still does not issue a session or call `/users/me`, confirms `/users/me` remains limited to `user_id` and `display_name`, and adds focused docs/source guards. No runtime/API/schema behavior changed — `docs/www-project-phase-96-registration-runtime-profile-boundary-review-v1.md`.

**Phase 97 (docs):** Minimal profile setup UI plan — defines a future post-login profile setup/edit UI that reads and writes existing `GET`/`PUT /users/me/profile` for `birth_year_month` and coarse `residential_region` only, requires sign-in with unauthenticated users guided to `/login`, uses full-replacement PUT with nullable fields, and excludes gender, exact birthday, precise location, auto-vote, vote recalculation, historical eligibility backfill, demographic breakdowns, ranking personalization, and analytics linkage. No runtime, frontend, API, schema, login/session, registration, `UserAuthResolver`, `/users/me` shape, Official Vote, Reference Answer, or vote token/counter behavior changes — `docs/www-project-phase-97-minimal-profile-setup-ui-plan-v1.md`.

**Phase 98:** Minimal profile setup UI runtime — adds post-login `GET /profile` and `profile-page.js` that call existing `GET`/`PUT /users/me/profile` with `credentials: 'same-origin'` for `birth_year_month` and coarse `residential_region` only. Unauthenticated visitors see neutral copy and `/login` guidance without mounting the form or calling the profile API; signed-in users can load, save, or clear both fields through full-replacement PUT. Shared chrome still uses `GET /users/me` for `display_name` only. No API, schema, login/session, registration, `UserAuthResolver`, `/users/me` shape, Official Vote, Reference Answer, or vote token/counter behavior changes — `docs/www-project-phase-98-minimal-profile-setup-ui-runtime-v1.md`.

**Phase 99:** Profile setup UI runtime review / hardening — reviews Phase 98 `/profile` runtime, shared chrome/login-state boundaries, and `GET`/`PUT /users/me/profile` call paths; adds focused source guards and `smoke:public:local` static checks confirming unauthenticated gating, same-origin two-field profile load/save, and exclusion of registration/login/session/vote/reference linkage. No runtime/API/schema behavior changed — `docs/www-project-phase-99-profile-setup-ui-runtime-review-v1.md`.

**Phase 100 (checkpoint):** Registration / login / profile milestone checkpoint — consolidates Phases 89–99 into the stable registration → login → profile setup baseline: `POST /registration` without session/`Set-Cookie`, explicit `POST /login/session`, `GET /users/me` limited to `user_id` + `display_name`, post-login `GET /profile` with `GET`/`PUT /users/me/profile` for two nullable eligibility fields only, and preserved vote/eligibility/Reference Answer/Raw Option Linkage Ban boundaries. Docs/checkpoint only; no runtime/API/schema behavior changed — `docs/www-project-phase-100-registration-login-profile-milestone-checkpoint-v1.md`.

**Phase 101 (docs):** Profile completion prompt plan — defines a future neutral post-login prompt that may guide signed-in users to `/profile` when `birth_year_month` or `residential_region` is null, using only `GET /users/me/profile` trigger data on homepage signed-in chrome, `/profile`, and a future vote-page pre-vote info zone; excludes `/registration`, auto-redirect, auto-vote, eligibility outcome copy, vote history/option linkage, and `/users/me` shape extension. Docs/spec only; no runtime, frontend, API, schema, login/session, registration, `UserAuthResolver`, Official Vote, Reference Answer, or vote token/counter behavior changes — `docs/www-project-phase-101-profile-completion-prompt-plan-v1.md`.

**Phase 102:** Profile completion prompt runtime — adds `profile-completion-prompt.js` and a homepage-only hook in `public-mvp-layout.js` that, after signed-in login-state read, calls `GET /users/me/profile` with `credentials: 'same-origin'` and shows a neutral `/profile` link when either `birth_year_month` or `residential_region` is null; hides when both are set; skips anonymous visitors and pages with `data-login-state-read="disabled"` (for example `/registration`). Shared chrome still uses `GET /users/me` for `display_name` only. No API, schema, login/session, registration, `UserAuthResolver`, Official Vote, Reference Answer, or vote token/counter behavior changes — `docs/www-project-phase-102-profile-completion-prompt-runtime-v1.md`.

**Phase 103:** Profile completion prompt runtime review / hardening — reviews Phase 102 `profile-completion-prompt.js`, homepage layout hook, static asset route, and prompt tests; adds focused source guards and `smoke:public:local` static checks confirming anonymous gating, homepage-only mount behind `shouldReadLoginState`, `/registration` opt-out, same-origin `GET /users/me/profile` null-completeness only, neutral non-blocking copy, and preserved shared-chrome/vote/Reference Answer/Raw Option Linkage Ban boundaries. No runtime/API/schema behavior changed — `docs/www-project-phase-103-profile-completion-prompt-runtime-review-v1.md`.

**Phase 104 (checkpoint):** Profile completion prompt milestone checkpoint — consolidates Phases 101–103 into the stable profile completion prompt baseline before Official Vote pre-vote eligibility UX: Phase 101 docs/spec plan, Phase 102 homepage-only `profile-completion-prompt.js` runtime with signed-in `GET /users/me/profile` null checks only, Phase 103 review/hardening with no runtime gap found, and preserved `/users/me` display_name-only chrome, `/registration` login-state-read opt-out, non-blocking non-redirect prompt behavior, vote-time eligibility, Reference Answer/Raw Option Linkage Ban boundaries. Docs/checkpoint only; no runtime/API/schema behavior changed — `docs/www-project-phase-104-profile-completion-prompt-milestone-checkpoint-v1.md`.

**Phase 105 (docs):** Official Vote pre-vote eligibility UX plan — defines future vote-page neutral hints for anonymous (login guidance to `/login` without `GET /users/me/profile`), signed-in incomplete profile (coarse birth year/month and residential region reminder to `/profile` without pass/fail), and signed-in complete profile (generic pre-submit notice without eligibility guarantee); generic vote failure/success copy without rule, option, token, or demographic leakage; explicit relationship to Phase 101–103 profile completion prompt as non-checker; and Raw Option Linkage Ban for pre-vote UX. Docs/spec only; no runtime, frontend, API, schema, login/session, registration, `UserAuthResolver`, vote evaluator, Official Vote transaction order, Reference Answer, or vote token/counter behavior changes — `docs/www-project-phase-105-official-vote-pre-vote-eligibility-ux-plan-v1.md`.

**Phase 106:** Official Vote pre-vote eligibility UX runtime — vote pages now show neutral pre-submit hints: anonymous users get `/login` guidance without `GET /users/me/profile`; signed-in users read `GET /users/me/profile` with `credentials: 'same-origin'` and only check whether `birth_year_month` or `residential_region` is null for a `/profile` nudge; complete profiles see a generic submit-time reminder. Vote failure copy is one neutral frontend message and success copy remains generic. No DB schema, migration, auth core, `UserAuthResolver`, `GET /users/me` shape, profile API behavior, registration/login session behavior, Official Vote transaction order, vote evaluator, `vote-by-index` option resolution order, Reference Answer, vote token/counter schema, demographic breakdown, or option linkage behavior changed — `docs/www-project-phase-106-official-vote-pre-vote-eligibility-ux-runtime-v1.md`.

**Phase 107:** Official Vote pre-vote UX runtime review / hardening — reviewed Phase 106 vote-page pre-vote hints and adjacent public UI surfaces, then added focused docs/tests guarding anonymous `/login` hints without profile reads, signed-in same-origin profile null checks only, non-blocking/no-auto-vote/no-option-resolution behavior, neutral copy, Raw Option Linkage Ban preservation, `/registration` opt-out, Reference Answer separation, and unchanged auth/vote backend boundaries. No runtime, API, schema, auth, Official Vote backend, vote evaluator, transaction order, or `vote-by-index` eligibility-before-option-resolve behavior changed — `docs/www-project-phase-107-official-vote-pre-vote-ux-runtime-review-v1.md`.

**Phase 108 (checkpoint):** Official Vote pre-vote UX milestone checkpoint — consolidates Phases 105–107 into the stable vote-page pre-vote hint baseline before further vote UX or Official Vote hardening: Phase 105 docs/spec plan, Phase 106 `official-vote-pre-vote-hints.js` runtime with anonymous `/login` guidance without profile reads and signed-in same-origin `birth_year_month`/`residential_region` null checks only, Phase 107 review/hardening with no backend gap found, preserved vote-time eligibility authority, non-blocking non-redirect pre-vote hints, unified neutral vote-failure copy, Reference Answer/Raw Option Linkage Ban boundaries, and recorded `smoke:public:local` Docker engine pipe unavailability as environment blocker. Docs/checkpoint only; no runtime/API/schema behavior changed — `docs/www-project-phase-108-official-vote-pre-vote-ux-milestone-checkpoint-v1.md`.

**Phase 109 (checkpoint):** Official Vote privacy guard checkpoint — reviews Official Vote, pre-vote UX, profile completion prompt, profile setup, registration/login boundaries, and adjacent public UI copy after Phase 108; adds focused privacy guards for Raw Option Linkage Ban preservation, page-local option selection memory, vote transaction/schema invariants, profile null-check-only prompts, `/users/me` display-name UI boundary, registration no-auto-login/session boundaries, Reference Answer separation, neutral copy, and no observability/analytics linkage. No runtime/API/schema/auth/vote backend behavior changed — `docs/www-project-phase-109-official-vote-privacy-guard-checkpoint-v1.md`.

**Phase 110 (docs):** Vote UX error handling polish plan — defines future vote-page neutral copy and UX boundaries for success, generic submit failure, anonymous and incomplete-profile pre-submit hints, possibly-ineligible reminders, duplicate-vote bucketing, poll lifecycle unavailability, network/server errors, and client-side missing-selection prompts; preserves vote-time evaluator authority, option-identity-free failure copy, Raw Option Linkage Ban, and registration/login/profile/vote/Reference Answer compatibility. Docs/spec only; no runtime, frontend, API, schema, auth, vote evaluator, or transaction-order behavior changes — `docs/www-project-phase-110-vote-ux-error-handling-polish-plan-v1.md`.

**Phase 111:** Vote UX error handling runtime polish — implements Phase 110 vote-page UX in `vote-page.js` and `public-mvp-ui.js`: generic success copy, single neutral submit-denial bucket, transport vs API failure separation, lifecycle-based submit disable with neutral blocked messages, load/transport neutral copy, client-only missing-selection prompt, and preserved page-local option clearing. Pre-vote hints remain in `official-vote-pre-vote-hints.js`. No backend, API, schema, auth, vote evaluator, transaction order, or Reference Answer behavior changes — `docs/www-project-phase-111-vote-ux-error-handling-runtime-polish-v1.md`.

**Phase 112 (checkpoint):** Vote UX error handling runtime review checkpoint — reviews Phase 111 vote-page success, failure, lifecycle-block, transport-error, missing-selection, and page-local option clearing runtime; adds focused docs/tests guarding generic success copy, single neutral submit-denial bucket, no submit-failure branching, transport/server payload isolation, lifecycle submit disable with neutral blocked messages, client-only missing-selection prompt, page-local state clearing on submit/pagehide/BFCache, pre-vote hint login/profile-completion-only boundary, and Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote backend behavior changed — `docs/www-project-phase-112-vote-ux-error-handling-runtime-review-checkpoint-v1.md`.

**Phase 113 (docs):** Results page empty / unavailable state UX plan — defines future `/results/:pollId` neutral copy and UX boundaries for aggregate-available (`revealed`/`locked`/`post_lock`), collecting, cancelled, unpublished, poll-not-found/unavailable, empty aggregate, and load/network/server failure states; preserves counter-free collecting/cancelled/unpublished boundaries, no voter-personal-state display, no option-linkage or internal identifier leakage, display-safe aggregate-only rendering, and vote/registration/login/profile/Reference Answer compatibility. Docs/spec only; no runtime, frontend, API, schema, auth, vote evaluator, result evaluator, or transaction-order behavior changes — `docs/www-project-phase-113-results-page-empty-unavailable-state-ux-plan-v1.md`.

**Phase 114:** Results page empty / unavailable state runtime polish — implements Phase 113 results-page UX in `result-page.js`: collecting `結果尚未公開` without counters or personal vote-state copy, lifecycle-owned cancelled/unpublished/unavailable messages without echoing backend `user_message`, neutral poll-unavailable and load-failure buckets, empty aggregate copy, and preserved display-safe aggregate rendering for `revealed`/`locked`/`post_lock`. No backend, API, schema, auth, vote evaluator, result evaluator, transaction order, or Reference Answer behavior changes — `docs/www-project-phase-114-results-page-empty-unavailable-state-runtime-polish-v1.md`.

**Phase 115 (checkpoint):** Results page runtime review checkpoint — reviews Phase 114 results-page collecting/cancelled/unpublished/unavailable/empty aggregate/load-failure runtime; adds focused docs/tests guarding display-safe aggregate-only rendering for `revealed`/`locked`/`post_lock`, counter-free lifecycle shells, no backend payload echo, no voter-personal-state or internal identifier copy, observability/linkage preservation, and vote/registration/login/profile/Reference Answer compatibility. No runtime/API/schema/auth/results backend behavior changed — `docs/www-project-phase-115-results-page-runtime-review-checkpoint-v1.md`.

**Phase 116 (docs):** Explore page empty / unavailable state UX plan — defines future `/explore` neutral copy and UX boundaries for live feed with collecting polls, empty feed, load/network/server failure, static homepage examples versus live feed separation, and non-collecting poll exclusion; preserves display-safe poll-summary-only rendering, no counters or result previews, no voter-personal-state or eligibility display, no option-linkage or internal identifier leakage, freshness-only non-personalized feed contract, and vote/results/registration/login/profile/Reference Answer compatibility. Docs/spec only; no runtime, frontend, API, schema, auth, vote evaluator, result evaluator, ranking, lifecycle, or feed behavior changes — `docs/www-project-phase-116-explore-page-empty-unavailable-state-ux-plan-v1.md`.

**Phase 117:** Explore page empty / unavailable state runtime polish — implements Phase 116 explore-page UX in `explore-page.js`, `explore.html`, and homepage static-example separation: `顯示公開問卷列表` populated feed status, `目前沒有正在收集中的公開問卷` empty state, neutral `目前無法載入探索列表，請稍後再試` load-failure bucketing without backend payload echo, `data-static-examples` homepage marker, collecting-only feed item guards, and preserved counter-free display-safe poll summary cards. No backend, API, schema, auth, vote evaluator, result evaluator, ranking, lifecycle, or feed behavior changes — `docs/www-project-phase-117-explore-page-empty-unavailable-state-runtime-polish-v1.md`.

**Phase 118 (checkpoint):** Explore page runtime review checkpoint — reviews Phase 117 explore-page populated/empty/load-failure/pagination-failure runtime; adds focused docs/tests guarding display-safe poll-summary-only cards, counter-free explore surface, static/live feed separation with `data-static-examples`, non-collecting payload exclusion, no backend payload or `error.message` echo, no voter-personal-state or internal identifier copy, observability/linkage preservation, and vote/results/registration/login/profile/Reference Answer compatibility. No runtime/API/schema/auth/feed backend behavior changed — `docs/www-project-phase-118-explore-page-runtime-review-checkpoint-v1.md`.

**Phase 119 (docs):** My polls empty / unavailable state UX plan — defines future `/my-polls` and `/my-polls?live=1` neutral copy and UX boundaries for not signed in, local/demo `creator_session` unavailable, owned-list empty, load/network/server failure, lifecycle-state labels (`draft`/`collecting`/`revealed`/`locked`/`cancelled`/`unpublished`), and mock/live dashboard separation; preserves creator-safe poll-summary-only rendering, no vote counters or result previews, no voter-personal-state or eligibility display, no option-linkage or internal identifier leakage, unchanged `creator_session` local/demo boundary, and vote/explore/results/registration/login/profile/Reference Answer compatibility. Docs/spec only; no runtime, frontend, API, schema, auth, creator ownership, lifecycle, vote evaluator, or result evaluator behavior changes — `docs/www-project-phase-119-my-polls-empty-unavailable-state-ux-plan-v1.md`.

**Phase 120:** My polls empty / unavailable state runtime polish — implements Phase 119 my-polls UX in `my-polls-page.js`, `my-polls.html`, and `creator-flow-copy.js`: `請先登入後查看你建立的問卷` sign-in-required bucketing, `目前無法載入你建立的問卷，請稍後再試` session/load failure copy without `error.message` echo, `你目前還沒有建立問卷` empty state, neutral lifecycle badges, creator-safe owned-poll payload guards, and mock/live dashboard separation with `data-mock-dashboard` / `data-live-owned-list`. No backend, API, schema, auth, creator ownership, lifecycle, vote evaluator, or result evaluator behavior changes — `docs/www-project-phase-120-my-polls-empty-unavailable-state-runtime-polish-v1.md`.

**Phase 121 (checkpoint):** My polls runtime review checkpoint — reviews Phase 120 my-polls not-signed-in, creator-session-unavailable, empty owned-list, load-failure, mock/live separation, creator-safe poll-summary-only rendering, neutral lifecycle labels (`草稿`/`收集中`/`已公開`/`公開鎖定期`/`鎖定期已結束`/`已取消`/`已下架`), no backend payload or `error.message` echo, no vote counters or result previews, no voter-personal-state or internal identifier copy, unchanged `creator_session` local/demo boundary, observability/linkage preservation, and vote/explore/results/registration/login/profile/Reference Answer compatibility. No runtime/API/schema/auth/creator ownership/lifecycle backend behavior changed — `docs/www-project-phase-121-my-polls-runtime-review-checkpoint-v1.md`.

**Phase 122 (checkpoint):** Vote page runtime review checkpoint — reviews `/vote/:id` empty/unavailable/load-failure/lifecycle-block/submit-failure and pre-vote hint runtime: anonymous login-only guidance without profile API reads, incomplete-profile `/profile` guidance without eligibility outcomes, complete-profile no-guarantee copy, `option_index`-only submit without early `option_id` resolution, unchanged `vote-by-index` eligibility-before-resolve boundary, counter-free vote surface, no backend payload echo, page-local option memory only, preserved `GET /users/me` (`user_id`/`display_name`) and `GET /users/me/profile` (`birth_year_month`/`residential_region`) boundaries, unchanged `creator_session` local/demo boundary, Reference Answer disconnect, and Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote backend behavior changed — `docs/www-project-phase-122-vote-page-runtime-review-checkpoint-v1.md`.

**Phase 123 (checkpoint):** Profile page runtime review checkpoint — reviews `/profile` unauthenticated/load-failure/save-failure runtime: unauthenticated path hides form, skips `GET /users/me/profile`, shows login guidance only; authenticated path uses same-origin `GET`/`PUT` with `birth_year_month`/`residential_region` only and nullable values; `readLoginState()` consumes `display_name` only; no eligibility inference or extra profile fields; frontend-owned failure copy without backend payload echo; preserved registration no-session/`POST /login/session` boundary, unchanged `creator_session` local/demo boundary, and Raw Option Linkage Ban preservation. No runtime/API/schema/auth/profile backend behavior changed — `docs/www-project-phase-123-profile-page-runtime-review-checkpoint-v1.md`.

**Phase 124 (checkpoint):** Login registration runtime review checkpoint — reviews `/registration` and `/login` auth-shell runtime: registration no auto-login/no Set-Cookie/no `GET /users/me` refresh with `data-login-state-read="disabled"`, POST body limited to `display_name`/`birth_year_month`/`residential_region` with credential in `Authorization` header, success guides to `/login`; login uses `POST /login/session` then `mountLoginStateRead()` → `GET /users/me` (`display_name` only); frontend-owned failure copy without backend payload echo; no eligibility inference or extra profile fields; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/registration/login backend behavior changed — `docs/www-project-phase-124-login-registration-runtime-review-checkpoint-v1.md`.

**Phase 125 (checkpoint):** Public navigation auth state runtime review checkpoint — reviews shared header/navigation chrome in `public-mvp-layout.js`, `login-state-read.js`, and `login-state-ui.js`: guest header shows visitor chip (`未登入`) with login/registration CTAs only; authenticated header shows `display_name` only via `GET /users/me`; no `user_id`/session/creator token/internal identifier display; auth read failures use neutral anonymous fallback without backend payload echo; `/registration` keeps `data-login-state-read="disabled"`; `/profile` unauthenticated path skips profile form and `GET /users/me/profile`; no eligibility/profile-completeness/vote-status/result-preview in header; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/navigation backend behavior changed — `docs/www-project-phase-125-public-navigation-auth-state-runtime-review-checkpoint-v1.md`.

**Phase 126 (checkpoint):** Logout session clear runtime review checkpoint — reviews header logout and session-clear runtime in `login-state-logout.js` and `login-state-ui.js`: logout button calls `DELETE /login/session` only; success resets header to anonymous/guest state without `display_name`/internal identifiers; logout/auth reread failures use neutral frontend copy without backend payload echo; logout does not use or clear `creator_session`; preserved `/users/me` (`user_id`/`display_name`), `/registration` no auto-login/`data-login-state-read="disabled"`, and `/profile` unauthenticated no-form/no-profile-API boundaries; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility; no eligibility/vote-status/analytics linkage; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/logout backend behavior changed — `docs/www-project-phase-126-logout-session-clear-runtime-review-checkpoint-v1.md`.

**Phase 127 (checkpoint):** Homepage profile prompt runtime review checkpoint — reviews homepage-only `mountProfileCompletionPrompt()` in `profile-completion-prompt.js` and `public-mvp-layout.js`: anonymous path skips `GET /users/me/profile` and completeness/eligibility display; signed-in homepage reads `GET /users/me` (header `display_name` only) then `GET /users/me/profile` (`birth_year_month`/`residential_region` only); completeness uses null checks only, not `can_vote`/`age_passed`/eligibility outcomes; neutral copy does not guarantee voting and CTA routes only to `/profile`; load failures use frontend-owned fallback without backend payload echo; preserved `/registration` no auto-login/`data-login-state-read="disabled"` and `POST /login/session` session boundary; unchanged `creator_session` local/demo boundary; no option choice read/store/display; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/profile-prompt backend behavior changed — `docs/www-project-phase-127-homepage-profile-prompt-runtime-review-checkpoint-v1.md`.

**Phase 128 (checkpoint):** Static public pages copy privacy boundary review checkpoint — reviews `/faq`, `/trust-levels`, homepage showcase cards, and adjacent static copy in `explore.html`, `registration.html`, `login.html`, `vote.html`, `results.html`, `my-polls.html`: FAQ preserves collecting hidden-count/reveal-lock boundaries and no creator per-vote-choice or demographic-breakdown implication; Trust Levels stays draft/not-open for credit score/points/rewards/paid promotion; homepage separates `data-static-examples` from live feed; registration/login copy preserves no auto-login and session boundary; Explore/My Polls/Results/Vote static copy avoids public vote-status/eligibility/internal-identifier leakage; unchanged `creator_session` local/demo boundary; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/static-page backend behavior changed — `docs/www-project-phase-128-static-public-pages-copy-privacy-boundary-review-checkpoint-v1.md`.

**Phase 129 (checkpoint):** Creator poll creation runtime review checkpoint — reviews `/polls/new` and `/polls/new?live=1` in `create-poll.html` and `create-poll-page.js`: static showcase (`parseLiveApiMode` false) uses `submitCreatePollDemo` without `/creator/*` calls; live flow bootstraps `creator_session` via `ensureCreatorSessionForLiveMode` then `POST /creator/polls` with poll-definition-only body (`title`/`description`/`options`/`category`/`eligible_rule_id`/`closes_at`/`publish`); disabled future eligibility/close fields stay out of payload; neutral API/session failure copy without backend payload echo; no vote/result/profile/Reference Answer reads; no vote counts/result previews/voter or eligibility status/internal identifiers on create page; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility elsewhere; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/creator-create backend behavior changed — `docs/www-project-phase-129-creator-poll-creation-runtime-review-checkpoint-v1.md`.

**Phase 130 (checkpoint):** Creator lifecycle controls runtime review checkpoint — reviews `poll-lifecycle-controls.js` and creator mounts on `/polls/new?live=1`, `/my-polls?live=1`, `/results/:id?creator=1`: cancel/close/unpublish via `POST /creator/polls/:id/{cancel,close,unpublish}` with `credentials: 'same-origin'` only; `lifecycleActionsForState` limits UI to collecting (`cancel`/`close`) and post_lock (`unpublish`); no frontend ownership/sessionStorage authority; `messageForLifecycleTransitionFailure` maps to neutral copy without backend payload echo; lifecycle panel shows policy/actions/links only—no counters/result previews/voter or eligibility status/internal identifiers; results creator panel refreshes display via existing result API path; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility elsewhere; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/lifecycle backend behavior changed — `docs/www-project-phase-130-creator-lifecycle-controls-runtime-review-checkpoint-v1.md`.

**Phase 131 (checkpoint):** Creator results panel runtime review checkpoint — reviews `/results/:id?creator=1` in `result-page.js` and `#creator-lifecycle-host`: `parseCreatorManageMode` gates creator panel only; plain `/results/:id` uses public `GET /polls/:id/results` without creator lifecycle mount; no `GET /creator/polls/:id/results` call (Phase 70 boundary); post-transition `refreshResultPageDisplay` re-fetches display-safe public results; collecting/cancelled/unpublished counter-free shells; revealed/locked/post_lock aggregate-only tiers; neutral load/refresh failure copy without backend payload echo; no ownership localStorage/sessionStorage authority; no voter lists/individual choices/internal identifiers; unchanged `creator_session` local/demo boundary; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/results-panel backend behavior changed — `docs/www-project-phase-131-creator-results-panel-runtime-review-checkpoint-v1.md`.

**Phase 132 (checkpoint):** Creator flow milestone review checkpoint — consolidates Phases 119–121 and 129–131 into the stable creator-flow baseline for `/polls/new`, `/my-polls`, lifecycle controls, and `/results/:id?creator=1`: static `/polls/new` showcase (`submitCreatePollDemo`, no `/creator/*`) vs live `?live=1` (`ensureCreatorSessionForLiveMode` + `POST /creator/polls` poll-definition-only body); `/my-polls?live=1` creator-safe owned summary only with neutral empty/unavailable/failure copy; lifecycle cancel/close/unpublish on `POST /creator/polls/:id/*` mounted from create/my-polls/results creator surfaces only; `/results/:id?creator=1` gates lifecycle panel only while public `GET /polls/:id/results` aggregate visibility stays unchanged; collecting/cancelled/unpublished counter-free; revealed/locked/post_lock display-safe aggregate only; no frontend ownership localStorage/sessionStorage authority; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility elsewhere; neutral error fallback without backend payload echo; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/creator-flow backend behavior changed — `docs/www-project-phase-132-creator-flow-milestone-review-checkpoint-v1.md`.

**Phase 133 (checkpoint):** Public participation / results flow milestone review checkpoint — consolidates Phases 113–118, 122, and 128 into the stable public participation baseline for `/explore`, `/vote/:id`, `/results/:id`, and static public copy: `/explore` freshness-only collecting feed via `GET /polls/feed` with `isExploreFeedItemSafe` counter-free cards; `/vote/:id` anonymous login-only hint without `GET /users/me/profile`, incomplete-profile `/profile` nudge without eligibility outcomes, complete-profile no-guarantee copy, `option_index`-only submit; `/results/:id` public `GET /polls/:id/results` without creator mode; collecting/cancelled/unpublished counter-free; revealed/locked/post_lock display-safe aggregate only; FAQ/static copy preserves collecting hidden-count, no creator per-choice implication, no vote guarantee; neutral error fallback without backend payload echo; preserved `/users/me` (`user_id`/`display_name`) and `/users/me/profile` (`birth_year_month`/`residential_region`) boundaries; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility elsewhere; vote-by-index eligibility-before-resolve and Official Vote transaction order unchanged; Reference Answer disconnect; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/public-participation backend behavior changed — `docs/www-project-phase-133-public-participation-results-flow-milestone-review-checkpoint-v1.md`.

**Phase 134 (checkpoint):** Auth / profile flow milestone review checkpoint — consolidates Phases 123–127 into the stable auth/profile baseline for `/registration`, `/login`, `/profile`, header auth state, logout, and homepage profile prompt: `/registration` no auto-login/Set-Cookie/`GET /users/me`, body limited to `display_name`/`birth_year_month`/`residential_region`, success guides to `/login`; `/login` via `POST /login/session` then header `GET /users/me` (`display_name` only); logout `DELETE /login/session` only with anonymous UI reset and no `creator_session` dependency; `/profile` unauthenticated no form/no profile API, authenticated `GET`/`PUT /users/me/profile` with nullable two-field boundary; homepage prompt homepage-only with anonymous no profile API, null-check completeness only, no vote guarantee; neutral error fallback without backend payload echo; unchanged `creator_session` local/demo boundary and explicit non-production `X-User-Id` compatibility elsewhere; Reference Answer disconnect; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/profile backend behavior changed — `docs/www-project-phase-134-auth-profile-flow-milestone-review-checkpoint-v1.md`.

**Phase 135 (polish):** Public error copy consistency polish — unifies neutral frontend-owned fallback copy across `/explore`, `/vote/:id`, `/results/:id`, `/my-polls?live=1`, `/login`, `/registration`, `/profile`, logout, homepage profile prompt, and creator lifecycle controls via `resolvePublicErrorUserMessage` allowlists; consistent load/unavailable punctuation; `/my-polls?live=1` distinguishes creator session failure (`CREATOR_SESSION_FAILURE`) from list load failure; catch handlers no longer echo foreign `error.message` or backend payloads; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-135-public-error-copy-consistency-polish-v1.md`.

**Phase 136 (checkpoint):** Public error copy runtime review checkpoint — reviews Phase 135 `resolvePublicErrorUserMessage` allowlist/fallback runtime across `/explore`, `/vote/:id`, `/results/:id`, `/my-polls?live=1`, `/login`, `/registration`, `/profile`, logout, homepage profile prompt, and creator lifecycle controls: allowlist-only user copy; foreign `error.message`/backend payload/internal code/stack trace not echoed; list failure vs `CREATOR_SESSION_FAILURE` distinguishable without backend detail; auth/profile boundaries unchanged (registration no auto-login/Set-Cookie/`GET /users/me`; `/users/me` and `/users/me/profile` field limits; `creator_session` non-production); vote-by-index eligibility-before-resolve and Official Vote transaction order unchanged; Reference Answer disconnect; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-136-public-error-copy-runtime-review-checkpoint-v1.md`.

**Phase 137 (polish):** Public loading / pending state UX polish — unifies safe frontend-owned loading / pending / submitting copy and busy-state handling across `/login`, `/registration`, `/profile`, `/polls/new?live=1`, `/vote/:id`, `/explore`, `/results/:id`, `/my-polls?live=1`, header logout, homepage profile prompt, and creator lifecycle controls via `PUBLIC_PENDING_USER_MESSAGES`, `setBusySubmitButton`, and `markRegionBusy`; disables action buttons while pending; resets busy state after success or failure; pending copy does not echo backend payloads or show option / eligibility / result leakage; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-137-public-loading-pending-state-polish-v1.md`.

**Phase 138 (checkpoint):** Public loading / pending state runtime review checkpoint — reviews Phase 137 `PUBLIC_PENDING_USER_MESSAGES` / `setBusySubmitButton` / `markRegionBusy` runtime across `/login`, `/registration`, `/profile`, `/polls/new`, `/vote/:id`, `/explore`, `/results/:id`, `/my-polls?live=1`, header logout, homepage profile prompt, and creator lifecycle controls: frontend-owned pending copy only; buttons disable while pending and reset on success/failure; vote submit pending does not reveal option linkage / eligibility / result preview / token / shard; vote-by-index body and Official Vote transaction order unchanged; explore/results/my-polls loading stays counter-free until display-safe aggregate; auth/profile boundaries unchanged (registration no auto-login/Set-Cookie/`GET /users/me`; `/users/me` and `/users/me/profile` field limits; `creator_session` non-production); no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-138-public-loading-pending-state-runtime-review-checkpoint-v1.md`.

**Phase 139 (polish):** Public success / completion state UX polish — unifies safe frontend-owned success / completion copy across `/login`, `/registration`, `/profile`, `/polls/new?live=1`, `/vote/:id`, `/my-polls?live=1` lifecycle controls, and header logout via `PUBLIC_SUCCESS_USER_MESSAGES` and shared success constants; login success confirms header state without `user_id`/session/token; registration success guides to login without auto-login/`Set-Cookie`/`GET /users/me`; profile save uses fixed copy without raw payload echo; create poll success shows safe next steps without creator token/internal id in messages; vote success is neutral only (no option/eligibility/result/token/shard); lifecycle success is neutral without creator token/internal id; logout resets auth UI without session/token message; success paths do not echo backend payloads; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-139-public-success-completion-state-polish-v1.md`.

**Phase 140 (checkpoint):** Public success / completion state runtime review checkpoint — reviews Phase 139 `PUBLIC_SUCCESS_USER_MESSAGES` / shared success constants runtime across `/login`, `/registration`, `/profile`, `/polls/new`, `/vote/:id`, `/my-polls?live=1` lifecycle controls, header logout, create-success share panel, and creator flow guide: frontend-owned success copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed on success paths; login success identifier-free; registration no auto-login/Set-Cookie/`GET /users/me`; profile save fixed copy without raw `birth_year_month`/`residential_region` echo; create poll success without creator token/internal id/backend payload in messages; vote success neutral only (no option/eligibility/result/token/shard); vote-by-index body and Official Vote transaction order unchanged; lifecycle success without creator token/session/internal id; logout silent UI reset without session/token message; auth/profile boundaries unchanged (`/users/me` and `/users/me/profile` field limits; `creator_session` non-production); no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-140-public-success-completion-state-runtime-review-checkpoint-v1.md`.

**Phase 141 (polish):** Public disabled / unavailable action copy polish — unifies safe frontend-owned disabled / unavailable copy across `/vote/:id`, `/results/:id`, `/explore`, `/my-polls?live=1` lifecycle controls, `/polls/new` demo, `/profile`, guest auth CTA, and pre-vote hints via `PUBLIC_UNAVAILABLE_USER_MESSAGES` and shared constants; vote blocked/route unavailable neutral only (no option/eligibility/result/token/shard); results collecting/cancelled/unpublished shells counter-free until display-safe aggregate; lifecycle action unavailable without creator token/internal id; explore empty/load-more and auth gates use fixed copy; disabled copy does not echo backend payloads; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-141-public-disabled-unavailable-action-copy-polish-v1.md`.

**Phase 142 (checkpoint):** Public disabled / unavailable action runtime review checkpoint — reviews Phase 141 `PUBLIC_UNAVAILABLE_USER_MESSAGES` / shared unavailable constants runtime across `/vote/:id`, `/results/:id`, `/explore`, `/my-polls?live=1`, `/profile`, `/polls/new` demo, guest auth CTA, pre-vote hints, and creator lifecycle controls: frontend-owned disabled/unavailable copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed on unavailable paths; vote blocked/route unavailable neutral only (no option/eligibility/result/token/shard); vote-by-index body and Official Vote transaction order unchanged; results collecting/cancelled/unpublished counter-free until `revealed`/`locked`/`post_lock` display-safe aggregate; lifecycle/profile/my-polls/auth CTA/create-poll unavailable without creator token/session/internal id/raw profile echo; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-142-public-disabled-unavailable-action-runtime-review-checkpoint-v1.md`.

**Phase 143 (polish):** Public CTA / link label consistency polish — unifies safe frontend-owned CTA / link labels across header auth, `/login`, `/registration`, profile prompt/page, `/vote/:id` pre-vote/success, `/results/:id`, `/explore`, `/my-polls?live=1`, `/polls/new` share/next-step, and creator lifecycle controls via `PUBLIC_CTA_LINK_LABELS` and shared `PUBLIC_CTA_*` constants; auth/profile/vote/results CTAs neutral only (no user_id/session/token/creator token/option/eligibility/aggregate preview); registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; CTA copy does not echo backend payloads; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-143-public-cta-link-label-consistency-polish-v1.md`.

**Phase 144 (checkpoint):** Public CTA / link label runtime review checkpoint — reviews Phase 143 `PUBLIC_CTA_LINK_LABELS` / shared `PUBLIC_CTA_*` constants runtime across header auth/nav, `/login`, `/registration`, profile prompt/page, `/vote/:id` pre-vote/success, `/results/:id`, `/explore`, `/my-polls?live=1`, `/polls/new` share/next-step, and creator lifecycle controls: frontend-owned CTA/link copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in CTA labels; auth/nav CTA without user_id/session/token/raw auth payload; registration no auto-login/Set-Cookie/`GET /users/me`; profile CTA without raw `birth_year_month`/`residential_region` echo; vote CTA neutral only (no option/eligibility/result/token/shard); vote-by-index body and Official Vote transaction order unchanged; results CTA without aggregate preview in link text until `revealed`/`locked`/`post_lock` display-safe aggregate; explore/my-polls/create-poll/lifecycle CTA without creator token/session/internal id/backend payload; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-144-public-cta-link-label-runtime-review-checkpoint-v1.md`.

**Phase 145 (polish):** Public status badge / state label consistency polish — unifies safe frontend-owned status badge / state labels across header auth chip, signed-in header, `/vote/:id` success, `/results/:id` collecting/unavailable shells, `/explore` feed cards, `/my-polls?live=1` lifecycle badges, `/polls/new` live submit/success, profile completion prompt, lifecycle state notes, and demo UI state preview via `PUBLIC_STATUS_LABELS`, `PUBLIC_POLL_LIFECYCLE_STATUS_LABELS`, and shared `PUBLIC_*_STATUS_*` constants; auth/profile status without user_id/session/token/raw profile echo; vote/results status neutral only (no option/eligibility/result/token/shard/aggregate preview outside display-safe states); registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; status copy does not echo backend payloads; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-145-public-status-badge-state-label-consistency-polish-v1.md`.

**Phase 146 (checkpoint):** Public status badge / state label runtime review checkpoint — reviews Phase 145 `PUBLIC_STATUS_LABELS` / `PUBLIC_POLL_LIFECYCLE_STATUS_LABELS` / `formatPublicPollLifecycleStatusLabel` runtime across header auth chip, signed-in header, `/vote/:id` success, `/results/:id` collecting/unavailable shells, `/explore` feed cards, `/my-polls?live=1` lifecycle badges, `/polls/new` live submit/success, profile completion prompt, lifecycle state notes, and demo UI state preview: frontend-owned status copy only; `formatPublicPollLifecycleStatusLabel` allowlist or draft fallback only; foreign `error.message`/backend payload/internal code/stack trace not echoed in status labels; auth/login chip without user_id/session/token/raw auth payload; profile prompt/status without raw `birth_year_month`/`residential_region` echo; vote status/success aria neutral only (no option/eligibility/result/token/shard); vote-by-index body and Official Vote transaction order unchanged; results collecting/cancelled/unpublished/unavailable status without aggregate preview until `revealed`/`locked`/`post_lock` display-safe aggregate; explore/my-polls/create-poll/lifecycle/demo status without creator token/session/internal id/backend payload; registration no auto-login/Set-Cookie/`GET /users/me`; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-146-public-status-badge-state-label-runtime-review-checkpoint-v1.md`.

**Phase 147 (polish):** Public empty / no data state UX polish — unifies safe frontend-owned empty / no-content copy across `/explore`, `/results/:id` aggregate empty, `/my-polls?live=1` creator poll list, creator flow empty headline, and lifecycle action-area empty notes via `PUBLIC_EMPTY_STATE_MESSAGES`, `PUBLIC_EMPTY_STATE_LABELS`, and shared `PUBLIC_*_EMPTY_*` constants; explore HTML empty panel synced on mount; empty copy neutral only (no vote counts/percentages/eligibility/option confirmation/backend payload echo); empty messages separated from `PUBLIC_UNAVAILABLE_USER_MESSAGES`; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-147-public-empty-no-data-state-ux-polish-v1.md`.

**Phase 148 (checkpoint):** Public empty / no data state runtime review checkpoint — reviews Phase 147 `PUBLIC_EMPTY_STATE_MESSAGES` / `PUBLIC_EMPTY_STATE_LABELS` / `syncExploreEmptyStatePanel` runtime across `/explore`, `/results/:id` aggregate empty, `/my-polls?live=1` creator poll list, creator flow empty headline, and lifecycle action-area empty notes: frontend-owned empty copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in empty-state messages; explore empty panel synced from shared constants without feed payload text; results empty aggregate gated to `revealed`/`locked`/`post_lock` display-safe aggregate only; my-polls/lifecycle empty without creator token/session/internal id; empty messages separated from `PUBLIC_UNAVAILABLE_USER_MESSAGES`; vote-by-index body and Official Vote transaction order unchanged; registration no auto-login/Set-Cookie/`GET /users/me`; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-148-public-empty-no-data-state-runtime-review-checkpoint-v1.md`.

**Phase 149 (polish):** Public help / hint text consistency polish — unifies safe frontend-owned help / hint / helper copy across `/login`, `/registration`, profile completion prompt, `/vote/:id` pre-vote hints, `/polls/new` demo/share panels, `/explore`, `/results/:id` intro, creator flow / lifecycle controls, and demo UI preview via `PUBLIC_HINT_TEXT_MESSAGES` and shared `PUBLIC_*_HINT` constants; hint copy neutral only (no vote counts/percentages/eligibility outcome/option confirmation/backend payload echo); registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-149-public-help-hint-text-consistency-polish-v1.md`.

**Phase 150 (checkpoint):** Public help / hint text runtime review checkpoint — reviews Phase 149 `PUBLIC_HINT_TEXT_MESSAGES` / `PUBLIC_*_HINT` runtime across `/login`, `/registration`, profile completion prompt, `/vote/:id` pre-vote hints, `/explore`, `/results/:id` intro, creator flow / lifecycle controls, and demo UI preview: frontend-owned hint copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in help / hint text; pre-vote hints guide login/profile only without eligibility outcome disclosure; results intro hints fixed scope-only without collecting-stage counter preview; creator flow hints without creator token/session/internal id; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; vote-by-index body and Official Vote transaction order unchanged; registration no auto-login/Set-Cookie/`GET /users/me`; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-150-public-help-hint-text-runtime-review-checkpoint-v1.md`.

**Phase 151 (polish):** Public form field label / placeholder consistency polish — unifies safe frontend-owned form field labels, placeholders, and field-level hints across `/login`, `/registration`, `/profile`, `/polls/new` demo/static create poll UI, and `/vote/:id` option legend via `PUBLIC_FORM_FIELD_LABELS`, `PUBLIC_FORM_PLACEHOLDERS`, `PUBLIC_FORM_FIELD_HINTS`, and shared `PUBLIC_FORM_*` constants with mount-time `sync*FormFieldCopy` helpers; shared birth year / month and region labels across registration/profile; form copy neutral only (no vote counts/eligibility outcome/internal id/backend payload echo); registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-151-public-form-field-label-placeholder-consistency-polish-v1.md`.

**Phase 152 (checkpoint):** Public form field label / placeholder runtime review checkpoint — reviews Phase 151 `PUBLIC_FORM_FIELD_LABELS` / `PUBLIC_FORM_PLACEHOLDERS` / `PUBLIC_FORM_FIELD_HINTS` runtime across `/login`, `/registration`, `/profile`, `/polls/new` demo/static create poll UI, and `/vote/:id` option legend: frontend-owned form copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in labels/placeholders/field hints; placeholders/hints describe collection scope without eligibility outcome disclosure or vote guarantee; static `login.html`/`registration.html`/`profile.html` shells align with shared constants; shared birth/region labels across registration/profile; registration no auto-login/Set-Cookie/`GET /users/me`; profile fields remain `birth_year_month`/`residential_region` only; vote-by-index body and Official Vote transaction order unchanged; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend changes — `docs/www-project-phase-152-public-form-field-label-placeholder-runtime-review-checkpoint-v1.md`.

**Phase 153 (polish):** Public section title / panel heading consistency polish — unifies safe frontend-owned section titles, panel headings, card headings, and form section headings across `/`, `/explore`, `/results/:id`, `/my-polls`, `/polls/new`, `/vote/:id`, `/login`, `/registration`, and `/profile` via `PUBLIC_SECTION_TITLES`, `PUBLIC_PANEL_HEADINGS`, `PUBLIC_CARD_HEADINGS`, `PUBLIC_FORM_HEADINGS`, and shared `PUBLIC_*` heading constants with mount-time `sync*PageSectionHeadings` helpers; heading copy neutral only (no vote counts/eligibility outcome/internal id/backend payload echo); registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-153-public-section-title-panel-heading-consistency-polish-v1.md`.

**Phase 154 (checkpoint):** Public section title / panel heading runtime review checkpoint — reviews Phase 153 `PUBLIC_SECTION_TITLES` / `PUBLIC_PANEL_HEADINGS` / `PUBLIC_CARD_HEADINGS` / `PUBLIC_FORM_HEADINGS` runtime across `/`, `/explore`, `/results/:id`, `/my-polls`, `/polls/new`, `/vote/:id`, `/login`, `/registration`, and `/profile`: frontend-owned heading copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in titles/headings; `public/index.html` `public-mvp-home.js` load safe for homepage heading sync only; results option list headings fixed (`不含票數`) without option-level linkage; vote `#poll-title` and results lifecycle titles stay outside static heading allowlists; registration no auto-login/Set-Cookie/`GET /users/me`; profile fields remain `birth_year_month`/`residential_region` only; vote-by-index body and Official Vote transaction order unchanged; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-154-public-section-title-panel-heading-runtime-review-checkpoint-v1.md`.

**Phase 155 (polish):** Public page intro / lead paragraph consistency polish — unifies safe frontend-owned page intro copy, lead paragraphs, and section leads across `/`, `/explore`, `/results/:id`, `/my-polls`, `/polls/new`, `/vote/:id`, `/login`, `/registration`, and `/profile` via `PUBLIC_PAGE_LEAD_PARAGRAPHS`, `PUBLIC_PAGE_LEADS`, `PUBLIC_PAGE_INTRO_TEXTS`, and shared `PUBLIC_*` lead constants with mount-time `sync*PageLeadParagraphs` helpers; intro copy neutral only (no vote counts/eligibility outcome/internal id/backend payload echo); vote `#poll-title`/`#poll-description` remain API-driven; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-155-public-page-intro-lead-paragraph-consistency-polish-v1.md`.

**Phase 156 (checkpoint):** Public page intro / lead paragraph runtime review checkpoint — reviews Phase 155 `PUBLIC_PAGE_LEAD_PARAGRAPHS` / `PUBLIC_PAGE_LEADS` / `PUBLIC_PAGE_INTRO_TEXTS` runtime across `/`, `/explore`, `/results/:id`, `/my-polls`, `/polls/new`, `/vote/:id`, `/login`, `/registration`, and `/profile`: frontend-owned intro / lead copy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in page leads; `PUBLIC_EXPLORE_PAGE_LEAD_HINT` safe alias of explore page lead; all nine static HTML shells align with shared constants; `renderResultsReadOnlyIntro` and creator section intro (`PUBLIC_CREATOR_*_LEAD_HINT`) fixed scope-only without collecting-stage counter preview; vote `#poll-title`/`#poll-description` stay outside static lead allowlists; registration no auto-login/Set-Cookie/`GET /users/me`; profile fields remain `birth_year_month`/`residential_region` only; vote-by-index body and Official Vote transaction order unchanged; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-156-public-page-intro-lead-paragraph-runtime-review-checkpoint-v1.md`.

**Phase 157 (polish):** Public microcopy / inline note consistency polish — unifies safe frontend-owned inline notes, microcopy, and supporting short copy across `/`, `/explore`, `/results/:id`, `/my-polls`, creator flow, and lifecycle controls via `PUBLIC_INLINE_NOTES`, `PUBLIC_MICROCOPY_MESSAGES`, `PUBLIC_SUPPORTING_NOTES`, and shared `PUBLIC_*` microcopy constants with mount-time `syncHomePageSupportingNotes` / `syncHomePageMicrocopy` / `syncExplorePageMicrocopy` helpers; microcopy neutral only (no vote counts/eligibility outcome/internal id/backend payload echo); my-polls demo row inline notes and share feedback centralized; lifecycle confirm microcopy centralized; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-157-public-microcopy-inline-note-consistency-polish-v1.md`.

**Phase 158 (checkpoint):** Public microcopy / inline note runtime review checkpoint — reviews Phase 157 `PUBLIC_INLINE_NOTES` / `PUBLIC_MICROCOPY_MESSAGES` / `PUBLIC_SUPPORTING_NOTES` runtime across `/`, `/explore`, `/results/:id`, `/my-polls`, creator flow, and lifecycle controls: frontend-owned microcopy only; foreign `error.message`/backend payload/internal code/stack trace not echoed in inline notes or microcopy; `syncHomePageSupportingNotes` / `syncHomePageMicrocopy` / `syncExplorePageMicrocopy` mount-time sync safe; my-polls demo inline notes / share feedback / aria labels on shared constants without internal id; lifecycle confirm copy and panel labels without token or internal state disclosure; `PUBLIC_RESULTS_PUBLIC_NOTICE_LABEL` fixed; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; registration no auto-login/Set-Cookie/`GET /users/me`; profile fields remain `birth_year_month`/`residential_region` only; vote-by-index body and Official Vote transaction order unchanged; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-158-public-microcopy-inline-note-runtime-review-checkpoint-v1.md`.

**Phase 159 (polish):** Public static HTML shell copy alignment polish — aligns public static HTML shells with centralized `PUBLIC_*` copy where Phase 135–158 runtime sync left stale shell wording: login form ready hint, registration success message, profile unauthenticated message, my-polls demo locked-row inline note and creator side note, create-poll policy「期中結果」wording, vote collecting notice body, results demo readonly title/brand on `/results/demo`, faq/trust-levels summary headings; minimal sync extensions in login/registration/profile/my-polls/vote/result page modules; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-159-public-static-html-shell-copy-alignment-polish-v1.md`.

**Phase 160 (checkpoint):** Public static HTML shell copy alignment runtime review checkpoint — reviews Phase 159 shell alignment across `login.html`, `registration.html`, `profile.html`, `my-polls.html`, `create-poll.html`, `vote.html`, `faq.html`, `trust-levels.html`, and mount-time sync extensions in login/registration/profile/my-polls/vote/result page modules: frontend-owned shell/sync copy only; `/results/demo` brand/h1 via path-aware `PUBLIC_RESULTS_DEMO_READONLY_TITLE`; homepage sample-section/footer embedded links intentionally not `textContent`-synced without inconsistency risk; policy list embedded links preserved; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; registration no auto-login/Set-Cookie/`GET /users/me`; vote-by-index body and Official Vote transaction order unchanged; no new logs/metrics/analytics/APM; Raw Option Linkage Ban preservation. No runtime/API/schema/auth/vote/result backend behavior changed — `docs/www-project-phase-160-public-static-html-shell-copy-alignment-runtime-review-checkpoint-v1.md`.

**Phase 161 (polish):** Public mobile visual rhythm & accessibility polish — improves shared `public-mvp.css` mobile spacing, card/panel/form readability, tap-target sizing (`--mvp-tap-min`), `focus-visible` rings, `touch-action: manipulation`, safe-area padding, and `prefers-reduced-motion` handling across public MVP pages; no copy centralization; `policy-ui-placeholders.js` / `HELP_COPY` unchanged; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-161-public-mobile-visual-rhythm-accessibility-polish-v1.md`.

**Phase 162 (polish):** Public desktop / tablet layout consistency polish — improves shared `public-mvp.css` tablet/desktop layout tokens (`--mvp-content-width`, gutter/grid gaps), `.mvp-page.mvp-shell` readable width alignment, card grid rhythm at `min-width: 641px` / `1024px`, form/panel/result shell spacing, footer width alignment with header, and preserves Phase 161 mobile rules; CSS-only (no HTML shell churn); no copy centralization; `policy-ui-placeholders.js` / `HELP_COPY` unchanged; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-162-public-desktop-tablet-layout-consistency-polish-v1.md`.

**Phase 163 (checkpoint):** Public frontend visual regression guard checkpoint — reviews and locks Phase 161–162 `public-mvp.css` invariants (mobile tap/focus/safe-area/reduced-motion/`max-width: 640px` single-column; tablet/desktop gutter/grid/`641px`/`1024px` breakpoints; `.mvp-page.mvp-shell` width; header/footer layout-width alignment; form/panel/result/empty-state shell classes) with consolidated guard tests; no new visual polish; no HTML shell churn; `policy-ui-placeholders.js` / `HELP_COPY` unchanged; registration boundary unchanged (no auto-login/Set-Cookie/`GET /users/me`); vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-163-public-frontend-visual-regression-guard-checkpoint-v1.md`.

**Phase 164 (checkpoint):** Public onboarding flow clarity review checkpoint — reviews registration/login/profile/homepage-prompt/vote pre-vote onboarding boundaries: `POST /registration` only (no Set-Cookie/auto-login/`GET /users/me`); `POST /login/session` + `GET /users/me` display_name-only UI read; `/profile` as sole `PUT /users/me/profile` edit surface; homepage `mountProfileCompletionPrompt` only after login-state on `/`; vote pre-vote hints guide anonymous→`/login` and incomplete→`/profile` without eligibility outcome disclosure; header/nav CTA consistency; consolidated guard tests; no new copy centralization or visual CSS polish; `policy-ui-placeholders.js` / `HELP_COPY` unchanged; vote-by-index body unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-164-public-onboarding-flow-clarity-review-checkpoint-v1.md`.

**Phase 165 (checkpoint):** Public creator poll creation flow review checkpoint — reviews and locks `/polls/new` and `/polls/new?live=1` boundaries: static demo vs live creator flow separation via `parseLiveApiMode` / `submitCreatePollDemo`; live path uses `creator_session` only (`ensureCreatorSessionForLiveMode` → `POST /creator/polls`); `creator_session` not production identity; production identity remains `UserAuthResolver` / login session (create runtime avoids `POST /login/session`, `GET /users/me`, `X-User-Id`); create payload limited to approved poll-definition fields; no option-choice + identity linkage; success share on public routes without token/counter/session leakage; lifecycle ownership unchanged; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; consolidated guard tests; no copy centralization or visual CSS polish; vote-by-index body and Official Vote transaction order unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-165-public-creator-poll-creation-flow-review-checkpoint-v1.md`.

**Phase 166 (checkpoint):** Public my-polls creator lifecycle UX review checkpoint — reviews and locks `/my-polls` and `/my-polls?live=1` boundaries: static mock dashboard vs live owned-list separation (`data-mock-dashboard` / `data-live-owned-list`); live path uses `creator_session` only (`prepareMyPollsLiveSession` → `GET /creator/polls`); `creator_session` not production identity; `CREATOR_OWNED_POLL_ALLOWED_KEYS` display-safe payloads (no vote counts/counter shards/tokens/voter or eligibility state); lifecycle actions limited to cancel/close/unpublish with results view via `/results/:id?creator=1`; empty/error/unavailable states use neutral copy without backend payload echo; creator ownership unchanged; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; consolidated guard tests; no copy centralization or visual CSS polish; vote-by-index body and Official Vote transaction order unchanged; no API/auth/session/vote/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-166-public-my-polls-creator-lifecycle-ux-review-checkpoint-v1.md`.

**Phase 167 (checkpoint):** Public results page visibility UX review checkpoint — reviews and locks `/results/:id` visibility boundaries including `?creator=1`: `collecting`/`cancelled`/`unpublished` counter-free; `revealed`/`locked`/`post_lock` display-safe aggregate only via `resolveResultRenderMode`; creator links use public `GET /polls/:id/results` without visibility bypass; empty/loading/error/refresh failures use neutral copy without backend payload echo; no user_id/session/token/shard/internal IDs in UI; Reference Answer remains outside UserAuthResolver/profile eligibility; `policy-ui-placeholders.js` / `HELP_COPY` remain separate policy-panel layer; consolidated guard tests; no copy centralization or visual CSS polish; vote-by-index body and Official Vote transaction order unchanged; no API/auth/session/vote/result evaluator backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-167-public-results-page-visibility-ux-review-checkpoint-v1.md`.

**Phase 168 (checkpoint):** Public explore feed UX review checkpoint — reviews and locks `/explore` feed boundaries: display-safe items via `EXPLORE_FEED_ALLOWED_ITEM_KEYS` / `isExploreFeedItemSafe`; no vote counts/result previews/voter or eligibility state/ranking/popularity/personalized recommendations; freshness-only `GET /polls/feed` ordering with `credentials: omit`; static homepage examples (`data-static-examples`) separated from live feed (`data-explore-feed="freshness-only"`); empty/loading/pagination/error states use neutral copy without backend payload echo; no user_id/session/token/shard/internal IDs in UI; `explore-page.js` does not import `policy-ui-placeholders.js` / `HELP_COPY`; consolidated guard tests; no copy centralization or visual CSS polish; vote-by-index body and Official Vote transaction order unchanged; no API/auth/feed/result evaluator backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-168-public-explore-feed-ux-review-checkpoint-v1.md`.

**Phase 169 (checkpoint):** Public MVP full-flow smoke checkpoint — reviews main public pages (`/`, `/registration`, `/login`, `/profile`, `/explore`, `/polls/new`, `/polls/new?live=1`, `/my-polls`, `/my-polls?live=1`, `/vote/:id`, `/results/:id`, `/faq`, `/trust-levels`) and confirms cross-flow UX/privacy/security boundaries: registration→login→profile→vote onboarding separated; registration no session/Set-Cookie/auto-login/`GET /users/me`; login session formal boundary; profile limited to `birth_year_month`/`residential_region`; explore freshness-only counter-free; create poll static vs `?live=1` creator flow; my-polls `creator_session` local/demo/test only; results lifecycle-tier visibility; vote pre-vote no eligibility/voter-state leakage; neutral error states; `HELP_COPY` policy-panel layer; consolidated guard tests + `smoke:public:local`; no copy centralization or visual CSS polish; vote-by-index body and Official Vote transaction order unchanged; no API/auth/vote/feed/result backend changes; Raw Option Linkage Ban preservation — `docs/www-project-phase-169-public-mvp-full-flow-smoke-checkpoint-v1.md`.

**Phase 170 (milestone):** Public MVP showcase readiness milestone — summarizes safe-to-demo public pages and fixed boundaries after Phases 135–169: lists `/` through `/trust-levels` as demo-safe within limits; separates demo/local/test `creator_session` from production identity; states explore freshness-only (not hot/ranking/personalized); results display-safe aggregate only for `revealed`/`locked`/`post_lock`; registration no auto-login/session; login/session formal boundary; profile `birth_year_month`/`residential_region` only; explicitly does not claim full production readiness; no backend/API/DB/auth/vote/result changes needed for milestone; milestone doc + guard tests; `smoke:public:local` recommended for local showcase; Raw Option Linkage Ban preservation — `docs/www-project-phase-170-public-mvp-showcase-readiness-milestone-v1.md`.

**Phase 171 (product spec):** Poll quality feedback mechanism product spec — docs/spec only for future post-vote lightweight optional feedback (`表達清楚`, `選項公平`, `值得思考`, `期待結果`, `題目不優`); clarifies `選項公平` consolidates `無刻意引導`/`中立客觀`, `表達清楚` covers question+options clarity, and `不太想答` is soft negative not public shame; feedback only after allowed post-vote UX point; no option-choice + user/session/device/request/log/trace/metric/error payload linkage; no logs/metrics/analytics/APM/ranking personalization/creator scoring runtime; does not change vote token/counter schema, Official Vote transaction order, vote-by-index, eligibility, result visibility, or Reference Answer; future-open questions on anonymous aggregate, `優質題目` qualification, negative-tag dominance, abuse prevention, and creator-facing delay/bucketing; **not implemented** — `docs/www-project-phase-171-poll-quality-feedback-mechanism-product-spec-v1.md`.

**Phase 172 (boundary spec):** Quality feedback privacy & abuse boundary spec — docs/spec only extending Phase 171: poll-level (not option-level) quality signal; forbids durable linkage between selected option↔user, selected option↔feedback tag, and feedback tag↔user/session/device/request/log/trace/metric/error payload; creator-facing feedback delayed/bucketed/thresholded/aggregate-only; `題目不優` soft negative not public shame or automatic punishment; `優質題目` qualification aggregate thresholds only (no identity-linked histories); abuse prevention via rate limits, coarse eligibility, delayed aggregation, minimum thresholds, anomaly review without voter exposure; no ranking personalization/analytics/creator score/punishment score/logs/metrics/APM/dashboards; confirms Official Vote transaction order, vote-by-index, vote token/counter schema, result visibility, Reference Answer, UserAuthResolver, profile fields unchanged; **not implemented** — `docs/www-project-phase-172-quality-feedback-privacy-abuse-boundary-spec-v1.md`.

**Phase 173 (data model plan):** Quality feedback data model planning spec — docs/spec only extending Phases 171–172: poll-level aggregate-only durable shape (`poll_id`, `feedback_tag`, `aggregate_count`, optional `updated_at`/threshold bucket state); no per-user feedback event rows; forbids `user_id`/`session_id`/`creator_session`/`vote_token`/`option_id`/`option_index`/`request_id`/device/IP/UA/trace/metric/error/analytics ids and selected-option+tag linkage; no feedback-to-vote-counter-shard connection; open choices on sync vs privacy-safe queue, short-lived non-durable dedup guard, creator display minimum threshold, `題目不優` dominance vs `優質題目`, feedback after failed/ineligible votes; explicit non-goals (no schema/migration/runtime/API/UI/analytics/ranking/creator punishment score); confirms Official Vote transaction order, vote-by-index, token/counter schema, result visibility, Reference Answer, UserAuthResolver, profile unchanged; **not implemented** — `docs/www-project-phase-173-quality-feedback-data-model-planning-spec-v1.md`.

**Phase 174 (API/runtime plan):** Quality feedback API & runtime boundary planning spec — docs/spec only extending Phases 171–173: feedback prompt only at allowed post-vote UX point; feedback submit separate from Official Vote transaction; future payload poll-level only (`poll_id`, `feedback_tags`) without option/identity/token/request/device/IP/UA/trace/metric/error/analytics fields; backend validates approved tags only and updates poll-level aggregates not vote counters/shards; runtime must not leak option existence/eligibility/counter beyond existing vote UX; failed/ineligible vote feedback documented as open question with safe options; frontend success/loading/error neutral (no backend payload/`error.message`/stack/internal code echo); creator read delayed/bucketed/thresholded/aggregate-only; no ranking/analytics/creator score/punishment score/logs/metrics/APM/dashboards; confirms vote token/counter schema, Official Vote transaction order, vote-by-index, eligibility, result visibility, Reference Answer, UserAuthResolver, profile unchanged; **not implemented** — `docs/www-project-phase-174-quality-feedback-api-runtime-boundary-planning-spec-v1.md`.

**Phase 175 (schema foundation plan):** Quality feedback schema foundation plan — docs/spec only extending Phases 171–174: future `poll_quality_feedback_aggregate` poll-level only (`poll_id`, `feedback_tag`, `aggregate_count`, optional `updated_at`/`threshold_state`/`bucket_state`); `UNIQUE (poll_id, feedback_tag)`; five approved MVP tags; `aggregate_count >= 0`; `poll_id` public poll identity without option linkage; forbids user/session/creator_session/vote_token/option/request/device/IP/UA/trace/metric/error/analytics/ranking id columns; no per-user event rows; no selected-option+tag linkage; no vote-counter-shard connection; open questions on `updated_at`, stored vs derived threshold/bucket state, pre-create vs insert-on-first-feedback, `題目不優` vs `優質題目`, privacy-safe queue, creator display minimum; explicit non-goals (no migration/schema implementation/runtime/API/UI/analytics/ranking/creator punishment score); confirms Official Vote transaction order, vote-by-index, token/counter schema, result visibility, eligibility, Reference Answer, UserAuthResolver, profile unchanged; **not implemented** — `docs/www-project-phase-175-quality-feedback-schema-foundation-plan-v1.md`.

**Phase 176-R (review checkpoint):** Quality feedback schema plan review checkpoint — reviews Phases 171–175 only; confirms poll-level aggregate-only feedback, no per-user event table, forbidden option/user/session/device/request/log/trace/metric/error linkages, `poll_quality_feedback_aggregate` direction (`poll_id`, `feedback_tag`, `aggregate_count`, `updated_at`), `UNIQUE (poll_id, feedback_tag)`, `aggregate_count >= 0`, five MVP tags; **approves Phase 177 minimal migration** excluding `threshold_state`/`bucket_state`; open runtime questions (pre-create vs insert-on-first, queue vs sync, failed-vote feedback, display threshold, `優質題目`) are not blockers; no analytics/logs/metrics/APM/ranking/creator punishment/dashboards; confirms Official Vote transaction order, vote-by-index, token/counter schema, result visibility, eligibility, Reference Answer, UserAuthResolver, profile unchanged; **no migration/schema/runtime/API/UI changes in this phase** — `docs/www-project-phase-176r-quality-feedback-schema-plan-review-checkpoint-v1.md`.

**Phase 177:** Quality feedback aggregate schema foundation — `migrations/012_phase177_quality_feedback_aggregate_foundation.sql` adds only `poll_quality_feedback_aggregate` with `poll_id`, constrained five-tag `feedback_tag`, non-negative `aggregate_count`, `updated_at`, and `UNIQUE (poll_id, feedback_tag)`; poll-level aggregate only; no per-user event table, option/user/session/device/request/log/trace/metric/error/analytics linkage, `threshold_state`, `bucket_state`, runtime API, ranking/dashboard/punishment/analytics behavior, or Official Vote / Reference Answer / auth / eligibility / result visibility changes — `docs/www-project-phase-177-quality-feedback-aggregate-schema-foundation-v1.md`.

**Phase 178-R (review checkpoint):** Quality feedback aggregate schema runtime readiness review checkpoint — reviews Phase 177 migration/guards/docs only; confirms poll-level aggregate, `poll_id` FK to polls only, DB five-tag allowlist, `aggregate_count BIGINT NOT NULL DEFAULT 0 CHECK >= 0`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `UNIQUE (poll_id, feedback_tag)`, no per-user event table/threshold/bucket/vote-token/counter-shard/option/user/session/request/trace linkage, no `src/` feedback runtime; **approves Phase 179 aggregate write API/runtime planning**; no migration/schema/runtime/API/UI changes in this phase — `docs/www-project-phase-178r-quality-feedback-aggregate-schema-runtime-readiness-review-checkpoint-v1.md`.

**Phase 179 (plan):** Quality feedback aggregate write API runtime plan — docs/spec only for future `POST /polls/:pollId/quality-feedback` that accepts body `{ "feedback_tag": "表達清楚" }` only, validates `pollId` + five-tag `feedback_tag`, increments only `(poll_id, feedback_tag)` in `poll_quality_feedback_aggregate`, returns only `{ "ok": true }`, and forbids option/user/session/device/request/log/trace/metric/error/analytics linkage, per-user event tables, durable duplicate prevention by user/session/device/request linkage, aggregate counts/threshold/bucket/ranking/creator-score responses, and any vote/auth/result/eligibility/Reference Answer/profile behavior change; **no runtime/API/DB/frontend implementation** — `docs/www-project-phase-179-quality-feedback-aggregate-write-api-runtime-plan-v1.md`.

**Phase 180:** Quality feedback aggregate write API runtime foundation — adds `POST /polls/:pollId/quality-feedback` with body `{ "feedback_tag": "表達清楚" }` only, five MVP tag validation, poll-existence check only, aggregate upsert into `poll_quality_feedback_aggregate` for `(poll_id, feedback_tag)`, and success response `{ "ok": true }` only; no schema/migration, per-user event table, duplicate prevention, option/vote/user/session/request/device/log/trace linkage, vote token/counter shard reads, result visibility/eligibility/auth/profile/Reference Answer changes, frontend feedback UX, analytics/ranking/dashboard/creator punishment, threshold, or bucket state — `docs/www-project-phase-180-quality-feedback-aggregate-write-api-runtime-foundation-v1.md`.

**Phase 180-R (review checkpoint):** Quality feedback write API runtime review checkpoint — reviews Phase 180 runtime/API/repository/tests/docs only; confirms `POST /polls/:pollId/quality-feedback` body `{ "feedback_tag" }` only and success `{ "ok": true }` only, five MVP tags, aggregate upsert `(poll_id, feedback_tag)` with insert count 1 / conflict `+1` / `updated_at = NOW()`, no new migration, no per-user event or option/user/session/vote/request/trace linkage, vote/auth/result/Reference Answer/profile boundaries unchanged; **approves Phase 181 post-vote quality feedback frontend UX**; no migration/schema/runtime/API/frontend changes in this phase — `docs/www-project-phase-180r-quality-feedback-write-api-runtime-review-checkpoint-v1.md`.

**Phase 181:** Post-vote quality feedback UX — adds post-vote feedback on `/vote/:pollId` success panel only (not before vote); five MVP tags (`表達清楚`, `選項公平`, `值得思考`, `期待結果`, `題目不優`); calls `POST /polls/:pollId/quality-feedback` with body `{ "feedback_tag" }` only; success/failure copy frontend-owned (no `aggregate_count`/threshold/bucket/ranking/creator score/result/option echo); page-local soft lock after success without `localStorage`/`sessionStorage`/cookie or durable dedup; feedback API does not know selected option; no analytics/log/trace linkage; demo polls keep preview-only placeholder; Official Vote / vote-by-index / eligibility / auth / Reference Answer / profile boundaries unchanged; Phase 182 may add review checkpoint — `docs/www-project-phase-181-post-vote-quality-feedback-ux-v1.md`.

**Phase 181-R (review checkpoint):** Post-vote quality feedback UX runtime review checkpoint — reviews Phase 181 frontend UX/tests/docs only; confirms feedback mounts only after vote success, demo polls skip live API, payload `{ "feedback_tag" }` only with `credentials: 'omit'`, five MVP tags, frontend-owned success/failure copy without response parsing, page-local soft lock without durable storage/dedup, no option/user/session/device/request/trace linkage, vote/auth/result/Reference Answer/profile boundaries unchanged; **approves Phase 182**; no migration/schema/runtime/API/frontend changes in this phase — `docs/www-project-phase-181r-post-vote-quality-feedback-ux-runtime-review-checkpoint-v1.md`.

**Phase 182 (milestone checkpoint):** Quality feedback milestone & governance boundary — docs/guards only; consolidates Phase 177–181-R delivery (`poll_quality_feedback_aggregate` schema, `POST /polls/:pollId/quality-feedback`, post-vote frontend UX, review checkpoints); records fixed contract (body `{ "feedback_tag" }` only, five MVP tags, response `{ "ok": true }` only, post-vote-only UX, demo skips API, `credentials: 'omit'`, page-local soft lock, no durable dedup/storage, no per-user event table, no option/user/session/device/request/log/trace/metric/error/analytics linkage, no `aggregate_count`/threshold/bucket/ranking/creator score echo); quality feedback is poll-level aggregate signal only; **does not add** 優質題目 badge, ranking, recommendation ordering, threshold/bucket, creator score, punishment/demotion, or abuse-prevention decisions; future 優質題目 or public/creator-facing presentation must open a new phase with governance rule, minimum volume, privacy/abuse boundary, display copy, and public vs creator-facing boundary; no migration/schema/runtime/API/frontend changes — `docs/www-project-phase-182-quality-feedback-milestone-governance-boundary-v1.md`.

**Phase 183 (governance spec):** High-quality poll presentation governance spec — docs/guards only; defines future **優質題目 / high-quality poll presentation** boundaries before any public-facing or creator-facing quality feedback presentation; explicitly no runtime/API/DB/frontend/migration/schema, no badge runtime, no ranking/recommendation ordering/hotness/trend/personalization, no threshold/bucket runtime, no creator score, no punishment/demotion/blocking/abuse decision, no public `aggregate_count`/tag count/tag breakdown/raw feedback total; keeps quality feedback poll-level aggregate signal only, bans per-user feedback event tables and option/user/session/device/request/log/trace/metric/error/analytics linkage, and requires a future phase with minimum volume, privacy, abuse, copy, display location, and rollback/removal rules before any implementation — `docs/www-project-phase-183-high-quality-poll-presentation-governance-spec-v1.md`.

**Phase 184 (eligibility plan):** High-quality poll badge eligibility plan — docs/guards only; plans future low-resolution boolean-like **優質題目** badge eligibility without runtime/API/DB/frontend/migration/schema, badge runtime, threshold/bucket runtime, ranking/recommendation ordering/hotness/trend/personalization, creator score, punishment/demotion/blocking/abuse decision, or public `aggregate_count`/tag count/tag breakdown/raw feedback total; limits future inputs to poll-level aggregate quality feedback, favors positive tags (`表達清楚`, `選項公平`, `值得思考`, `期待結果`), treats `題目不優` only as future conservative/manual governance direction, bans option/user/session/device/request/log/trace/metric/error/analytics linkage and creator-level scoring, requires minimum volume without writing a runtime constant, and requires silent public absence rather than `尚未達標`/`回饋不足`/`品質不足` states when not qualified — `docs/www-project-phase-184-high-quality-poll-badge-eligibility-plan-v1.md`.

**Phase 185-R (review checkpoint):** High-quality poll badge eligibility plan review checkpoint — reviews Phase 184 plan against Phase 182/183 governance only; confirms plan-only posture with no runtime/API/DB/frontend/migration/schema, no badge/threshold/bucket runtime, no ranking/recommendation ordering/hotness/trend/personalization, no creator score/punishment/demotion/blocking/abuse decision, no public `aggregate_count`/tag count/tag breakdown/raw feedback total, silent public absence when not qualified, creator-facing low-volume inference bans, poll-level aggregate signal only, no per-user event table or option/user/session/device/request/log/trace/metric/error/analytics linkage, vote/auth/result/Reference Answer/profile/lifecycle boundaries unchanged; **APPROVED — Phase 186 blockers: none identified**; next phase approved only as plan-only **High-quality Poll Badge Read-model / Presentation Plan** (no badge runtime) — `docs/www-project-phase-185r-high-quality-poll-badge-eligibility-plan-review-checkpoint-v1.md`.

**Phase 186 (plan):** High-quality poll badge read-model / presentation plan — docs/guards only; plans future public-facing badge read-model ceiling (`quality_badge: null` or `quality_badge: "positive_feedback"` only) without runtime/API/DB/frontend/migration/schema, badge runtime, read model runtime, public API response field, frontend badge DOM/copy/class/id, threshold/bucket runtime, ranking/recommendation ordering/hotness/trend/personalization, creator score, punishment/demotion/blocking/abuse decision, or public `aggregate_count`/tag count/tag breakdown/raw feedback total/score/rank/percentile; unifies not-qualified/low-volume/not-computed/gated cases to `null` without reason distinction; public frontend `null` means silent no-badge (no `尚未達標`/`回饋不足`/`品質不足`); positive low-resolution copy only (e.g. `回饋良好`); creator-facing read model must open separate phase; future read model must not change feed/explore ordering, results visibility, or lifecycle; future implementation requires separate review checkpoint before runtime — `docs/www-project-phase-186-high-quality-poll-badge-read-model-presentation-plan-v1.md`.

**Phase 186-R (review checkpoint):** High-quality poll badge read-model presentation plan review checkpoint — reviews Phase 186 plan against Phase 182–185-R governance only; confirms plan-only posture with no runtime/API/DB/frontend/migration/schema, no badge/read model runtime/public API response field/frontend badge DOM-copy-class-id, future output ceiling `quality_badge: null` or `"positive_feedback"` only, unified null without reason distinction for low-volume/not-qualified/not-computed/gated cases, silent public no-badge on null, creator-facing read model separate phase, no feed/explore ordering/results visibility/lifecycle change, poll-level aggregate signal only, no per-user event table or option/user/session/device/request/log/trace/metric/error/analytics linkage, vote/auth/result/Reference Answer/profile/lifecycle boundaries unchanged; **APPROVED — Phase 187 blockers: none identified**; next phase approved only as plan-only **High-quality Poll Badge Runtime Implementation Plan** (no badge runtime/API/frontend/schema) — `docs/www-project-phase-186r-high-quality-poll-badge-read-model-presentation-plan-review-checkpoint-v1.md`.

**Phase 187 (plan):** High-quality poll badge runtime implementation plan — docs/guards only; plans future minimal public-facing badge runtime route (`quality_badge: null` or `quality_badge: "positive_feedback"` only) without runtime/API/DB/frontend/migration/schema, badge runtime, public API response field, frontend badge DOM/copy/class/id, threshold/bucket runtime, ranking/recommendation ordering/hotness/trend/personalization, creator score, punishment/demotion/blocking/abuse decision, or public `aggregate_count`/tag count/tag breakdown/raw feedback total/score/rank/percentile; unifies not-qualified/low-volume/not-computed/gated cases to `null` without reason distinction; public frontend `null` means silent no-badge (no `尚未達標`/`回饋不足`/`品質不足`/`低品質`); positive low-resolution copy only (e.g. `回饋良好`); creator-facing explanation/debug must open separate phase; no durable badge table (cache/materialized state requires separate schema/migration phase); first-version runtime should derive from `poll_quality_feedback_aggregate` only with boolean-like output and no derivation details; future implementation requires Phase 187-R review checkpoint before runtime — `docs/www-project-phase-187-high-quality-poll-badge-runtime-implementation-plan-v1.md`.

**Phase 187-R (review checkpoint):** High-quality poll badge runtime implementation plan review checkpoint — reviews Phase 187 plan against Phase 182–186-R governance only; confirms plan-only posture with no runtime/API/DB/frontend/migration/schema, no badge runtime/public API response field/frontend badge DOM-copy-class-id, future output ceiling `quality_badge: null` or `"positive_feedback"` only, unified null without reason distinction for low-volume/not-qualified/not-computed/gated cases, first runtime from `poll_quality_feedback_aggregate` only with boolean-like output, no durable badge table (cache/materialized state requires separate schema/migration phase), creator-facing explanation/debug separate phase, no feed/explore ordering/results visibility/lifecycle change, poll-level aggregate signal only, no per-user event table or option/user/session/device/request/log/trace/metric/error/analytics linkage, vote/auth/result/Reference Answer/profile/lifecycle boundaries unchanged; **APPROVED — Phase 188 blockers: none identified**; next phase approved only as **High-quality Poll Badge Minimal Public Read Runtime** (public API/read helper `quality_badge` field only; no schema/migration; no frontend badge presentation) — `docs/www-project-phase-187r-high-quality-poll-badge-runtime-implementation-plan-review-checkpoint-v1.md`.

**Phase 188 (runtime):** High-quality poll badge minimal public read runtime — adds `quality_badge: null | "positive_feedback"` to `GET /polls/:id`, `GET /polls/:id/results`, and `GET /polls/feed` via `deriveQualityBadge()` from existing `poll_quality_feedback_aggregate` only; no schema/migration, durable badge table, frontend badge presentation, creator-facing explanation/debug, or public `aggregate_count`/tag count/tag breakdown/raw feedback total/score/rank/percentile/threshold_state/bucket_state/creator_score; unifies not-qualified/low-volume/not-computed/gated cases to `null` without reason distinction; explore feed parsing tolerates `quality_badge` only (no DOM/copy/rendering); feed/explore ordering, result visibility, lifecycle, auth, vote transaction, vote-by-index, eligibility, Reference Answer, and UserAuthResolver unchanged — `docs/www-project-phase-188-high-quality-poll-badge-minimal-public-read-runtime-v1.md`.

**Phase 188-R (review checkpoint):** High-quality poll badge minimal public read runtime review checkpoint — reviews Phase 188 runtime against Phase 182–187-R governance only; confirms public API ceiling `quality_badge: null` or `"positive_feedback"` only on `GET /polls/:id`, `GET /polls/:id/results`, and `GET /polls/feed`; `deriveQualityBadge()` boolean-like without reason/count/score/threshold/bucket leakage; repository read-only on `poll_quality_feedback_aggregate` without user/session/device/request/option joins; no schema/migration/durable badge table; no public `aggregate_count`/tag breakdown/raw feedback total/score/rank/percentile/threshold_state/bucket_state/creator_score; feed/explore ordering/results visibility/lifecycle/auth/vote transaction/vote-by-index/eligibility/Reference Answer/UserAuthResolver/profile boundaries unchanged; frontend parsing tolerance only (no badge DOM/copy/rendering/class/id; no localStorage/sessionStorage/cookie for quality badge); poll-level aggregate signal only with no per-user event table or option/user/session/device/request/log/trace/metric/error/analytics linkage; **APPROVED — Phase 189 blockers: none identified**; next phase approved only as plan-only **High-quality Poll Badge Frontend Presentation Plan** (no frontend badge rendering runtime implementation) — `docs/www-project-phase-188r-high-quality-poll-badge-minimal-public-read-runtime-review-checkpoint-v1.md`.

**Quality question incentive draft (docs, policy only — not implemented):** Creator levels, daily poll limits, quality signals, abuse rules, MVP “document and mock UI first” — `docs/www-project-quality-question-incentive-policy-draft-v1.md`. No scoring schema or API in this draft.

**Phase 28:** Shared lightweight stylesheet `public/frontend/public-mvp.css` for all public MVP pages (mobile-friendly layout; no UI framework).

**Spec note:** Agent spec **§32 Phase 5 (Wonder Flow / Ranking) is not fully complete.** Phases 5B–5C deliver only `GET /polls/feed` (public, non-personalized, freshness-only; no answer-direction signals).

**Admin / governance (Phase 6B–12):** Typo correction workflow, Dual-Admin decisions, apply, suspended correction with public notice **write**, safe **audit read** routes, blind `review-context` (`decision_summary` only; no `peer_decisions` / `final_decisions` / admin IDs / reason fields), poll-scoped public notice **read + display**, the safe global `GET /admin/correction-audit` queue, and a server-side opaque Bearer token + RBAC v1 boundary are **implemented**. Full login/session/JWT/OAuth management, real Spread Score calculation, and semantic typo detection are **not** implemented — see `docs/admin-correction-http.md` and `docs/www-project-milestone-phase-12-handoff-v1.md`.

## Prerequisites

- Node.js 20+
- PostgreSQL (optional for `npm run migrate`; required from Phase 1 onward)

## Commands

```bash
npm install
npm run typecheck
npm run build
npm start
# One explicit scheduler batch after build; requires DATABASE_URL:
# DATABASE_URL=postgres://... npm run scheduler:lifecycle -- --limit 100
npm test
npm run migrate:check
# Apply migrations when DATABASE_URL is set:
# DATABASE_URL=postgres://... npm run migrate
```

`npm test` stays **DB-free**. Optional PostgreSQL integration tests (local/manual, **PostgreSQL 17+**, isolated database **`www_test` only**). Full setup, safety checks, and single-file runs: **`docs/www-project-phase-15-pg-integration-test-setup-v1.md`**.

```bash
npm run test:integration:local
npm run smoke:admin:local
npm run smoke:public:local
npm run demo:public:local
```

`npm run demo:public:local` starts Docker `www_test` if needed, applies migrations, seeds **local-only** fake official demo voters (for reliable manual voting on `127.0.0.1`), sets fake `ADMIN_AUTH_CREDENTIALS_JSON` in the process only, then listens on port 3000 until Ctrl+C. Refuses non-`www_test` URLs. Does not commit secrets.

The integration quick command starts the local Docker test service if needed, waits for health, runs migration validation and apply, then runs the integration suite. The admin smoke command uses the same isolated `www_test` database with fake local-only admin credentials and synthetic fixture rows. The public flow smoke command validates `GET /` → `/polls/new` → create poll → `/vote/:pollId` → vote-by-index → `/results/:pollId` without a browser, and checks that public JSON responses do not expose `option_id`, vote tokens, or shard internals. All local smoke commands intentionally leave the test container running for fast reruns. If `DATABASE_URL` is unset, the lower-level `npm run test:integration` exits immediately with setup instructions (environment not ready — not a unit-test regression). See Phase 15 doc §8 and Phase 14 doc §8.

`npm run scheduler:lifecycle -- --limit 100` is a one-shot operator command, not an HTTP hook or always-on worker. It requires a migrated `DATABASE_URL`, accepts limits `1..100`, prints counts and aggregated lifecycle error codes only, and returns nonzero when malformed lifecycle rows fail closed. Cron frequency, overlap policy, deployment supervision, alerting, and retries remain operator responsibilities.

## Layout

- `src/` — application code
- `tests/` — Vitest suites
- `migrations/` — ordered PostgreSQL migrations
- `docs/` — implementation specs and operator docs (e.g. `docs/admin-correction-http.md`)

## Public APIs (Phase 0–5C)

Legacy public poll creator-write routes no longer accept `X-User-Id` creator authority; they return `410 LEGACY_CREATOR_WRITE_RETIRED`. Public vote and Reference Answer routes keep their existing public `X-User-Id` behavior. Live creator frontend flows use `/creator/polls` with the `creator_session` cookie instead.

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/polls` | Retired legacy creator-write create route; returns `410 LEGACY_CREATOR_WRITE_RETIRED` |
| `POST` | `/creator/polls` | Live creator create route using `creator_session` cookie |
| `GET` | `/creator/polls` | Counter-free owned poll list using `creator_session` cookie |
| `DELETE` | `/creator/polls/:id` | Creator-owned soft-delete route using `creator_session` cookie |
| `GET` | `/registration` | Minimal production registration form; calls `POST /registration` and then guides to `/login` |
| `POST` | `/registration` | Production registration foundation; verifier-backed account/profile setup only, no session cookie |
| `POST` | `/login/session` | Production login submit foundation; requires configured `trustedCredentialVerifier`, stores only `user_sessions.token_sha256`, and issues `www_session` |
| `DELETE` | `/login/session` | Revoke current valid `www_session` by digest and clear the cookie |
| `GET` | `/users/me` | Read minimal authenticated identity (`user_id`, `display_name`) through `UserAuthResolver` |
| `GET` | `/users/me/profile` | Read current user's profile fields (`birth_year_month`, `residential_region`) |
| `PUT` | `/users/me/profile` | Replace current user's profile fields; accepts only `birth_year_month` and coarse `residential_region` |
| `GET` | `/polls/:id` | Poll detail (no vote/ranking signals) |
| `DELETE` | `/polls/:id` | Retired legacy creator-write delete route; returns `410 LEGACY_CREATOR_WRITE_RETIRED` |
| `POST` | `/polls/:id/cancel` | Retired legacy creator-write lifecycle route; returns `410 LEGACY_CREATOR_WRITE_RETIRED` |
| `POST` | `/polls/:id/close` | Retired legacy creator-write lifecycle route; returns `410 LEGACY_CREATOR_WRITE_RETIRED` |
| `POST` | `/polls/:id/unpublish` | Retired legacy creator-write lifecycle route; returns `410 LEGACY_CREATOR_WRITE_RETIRED` |
| `POST` | `/polls/:id/reference-answer` | Record Reference Answer participation only |
| `POST` | `/polls/:id/vote` | Record Official Vote and increment aggregate shard |
| `POST` | `/polls/:id/vote-by-index` | Record Official Vote from a public option index; internal option ID stays server-side |
| `GET` | `/polls/:id/results` | Display-safe Official Vote results; aggregate only for `revealed` / `locked` / `post_lock`; `collecting` / `cancelled` / `unpublished` / `draft` are counter-free shells |
| `GET` | `/polls/feed` | Public freshness-only discovery feed (5B–5C; see below) |
| `GET` | `/polls/:id/public-notices` | Read visible public correction notices (Phase 8) |
| `GET` | `/results/:id` | Public result page; **`?creator=1`** shows creator lifecycle panel (MVP dev; backend remains authoritative) |
| `GET` | `/` | Public landing page (entry to create poll flow) |
| `GET` | `/explore` | Freshness-only public explore UI (consumes `GET /polls/feed`; collecting polls only; no counters) |
| `GET` | `/faq` | Static FAQ (policy-aligned Traditional Chinese; demo-facing) |
| `GET` | `/trust-levels` | Static trust-level permission matrix (demo-facing) |
| `GET` | `/profile` | Profile UX for `birth_year_month` and coarse `residential_region` (MVP `X-User-Id`) |
| `GET` | `/my-polls` | Creator dashboard; default static mock table; **`?live=1`** for live management (MVP dev) |
| `GET` | `/vote/demo` | Static vote policy shell (`?ui_state=`, `?nav=`) |
| `GET` | `/results/demo` | Static results policy shell (`?ui_state=`, `?nav=`) |
| `GET` | `/polls/new` | Public poll creation UI; default demo (no DB write); **`?live=1`** for real `POST /creator/polls` (MVP dev) |
| `GET` | `/vote/:id` | Minimal public voting UI |
| `GET` | `/health` | Health check |
| `GET` | `/frontend/*` | Static frontend assets (JS, shared `public-mvp.css`) |

`PUT` / `PATCH` on polls return `405` (creator zero-edit after publish).

Minimal public poll creation UI: `GET /polls/new` (demo by default). With **`?live=1`**, it ensures the local/demo creator session when available, submits to `POST /creator/polls`, shows share links, and wires creator lifecycle controls. Neither mode adds production login, ranking, or post-publish edit behavior.

Minimal public voting UI: `GET /vote/:id`. It loads public poll detail, submits the selected public `option_index` to `POST /polls/:id/vote-by-index`, and links to the identity-neutral result page without exposing internal option IDs.

**Minimal public flow (Phase 23–24):**

1. `GET /` — landing page; primary circulation is **share links** (vote/results URLs)
2. `GET /explore` — freshness-only explore list (`GET /polls/feed`); collecting polls only; no vote counts or ranking
3. `GET /polls/new` — demo/static create UI by default (no DB write); local MVP real create uses **`/polls/new?live=1`** (`POST /creator/polls`)
4. After **`?live=1`** create success, shareable full URLs for `GET /vote/:pollId` and `GET /results/:pollId` (copy buttons + visible links; poll id only, no tokens)
5. `GET /vote/:pollId` — vote; success links to results
6. `GET /results/:pollId` — **read-only** display-safe results per `public_lifecycle_state` (collecting counter-free; aggregate when `revealed` / `locked` / `post_lock`); no login, feed UI, ranking, or admin UI (Phase 29–30). Creator lifecycle: **`?creator=1`** (MVP dev; see Phase 60 handoff)

Manual browser checklist (Traditional Chinese): **`docs/www-project-public-mvp-manual-qa-v1.md`**.

Demo/release handoff (Traditional Chinese, showcase + boundaries): **`docs/www-project-public-mvp-demo-release-handoff-v1.md`**.

Cross-browser QA log (Traditional Chinese, PASS/WARN/FAIL tables for real devices): **`docs/www-project-public-mvp-cross-browser-qa-log-v1.md`**.

**Production readiness boundary (Traditional Chinese, demo vs production gates; not a deploy completion proof):** **`docs/www-project-production-readiness-boundary-v1.md`**.

**Local demo startup (Traditional Chinese):** preferred: `npm run demo:public:local` → open `http://127.0.0.1:3000/`. Manual path: session `DATABASE_URL` on `www_test`, seed demo users, migrate, build, `npm start`. Step-by-step: **`docs/www-project-local-demo-startup-v1.md`** (not for production deploy). Public MVP UI is a functional CSS skeleton; full visual redesign is a later UI Phase.

### Demo release readiness (Phase 69)

**Status:** MVP demo is **showcase-ready** for local handoff — creator flow, profile eligibility, public vote/results, and integrated manual QA are documented and guarded. **Phase 70 route adapters** resolve production identity through `UserAuthResolver`; Phase 71–73 covered **formal session lifecycle** and **production login/session UX planning**; **Phase 80–96** add minimal production login submit, digest-backed `www_session` issuance/revoke, `UserAuthResolver` session read integration, `GET /users/me`, login-state read/logout UI hooks, a minimal `/login` credential-proof submit form, production registration foundation, registration UI plan, minimal `/registration` form runtime, registration UI runtime hardening, registration/login navigation polish, registration/login smoke coverage, and registration runtime/profile boundary review. This is still **not** production-deploy-ready: the login/registration flows require an approved backend verifier/origin configuration, local/demo visitors still use **MVP demo-style `X-User-Id`**, and creators use **`creator_session`** (non-production identity). The old **production user-auth wiring later** milestone is now partially implemented through backend session-read, minimal frontend login-state surfaces, and registration runtime foundation. See **`docs/www-project-phase-96-registration-runtime-profile-boundary-review-v1.md`**, **`docs/www-project-phase-95-registration-login-full-flow-smoke-coverage-v1.md`**, **`docs/www-project-phase-94-registration-login-navigation-copy-polish-v1.md`**, **`docs/www-project-phase-93-registration-ui-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-92-minimal-registration-form-runtime-v1.md`**, **`docs/www-project-phase-91-minimal-registration-ui-plan-v1.md`**, **`docs/www-project-phase-90-registration-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-89-registration-runtime-foundation-v1.md`**, **`docs/www-project-phase-87-production-login-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-86-minimal-production-login-form-runtime-v1.md`**, **`docs/www-project-phase-85-production-login-ui-plan-v1.md`**, **`docs/www-project-phase-84-frontend-logout-ui-hook-v1.md`**, **`docs/www-project-phase-83-frontend-login-state-read-v1.md`**, **`docs/www-project-phase-82-authenticated-me-endpoint-foundation-v1.md`**, **`docs/www-project-phase-81-user-auth-resolver-session-read-integration-v1.md`**, **`docs/www-project-phase-80-production-login-submit-runtime-foundation-v1.md`**, **`docs/www-project-phase-78-production-session-schema-foundation-v1.md`**, **`docs/www-project-phase-77-production-login-session-runtime-plan-v1.md`**, **`docs/www-project-phase-73-production-login-session-ux-plan-v1.md`**, **`docs/www-project-phase-72-production-credential-verifier-foundation-v1.md`**, **`docs/www-project-phase-71-production-credential-verifier-plan-v1.md`**, and **`docs/www-project-phase-70-final-production-auth-boundary-checkpoint-v1.md`**.

**Recommended tester entry:** **`docs/www-project-phase-69-mvp-demo-release-readiness-handoff-v1.md`** → startup → operation order → release readiness checklist. Detailed steps: manual QA §3.10 · demo handoff · Phase 60/67/68 docs (cross-linked, not duplicated).

### Public demo test order (Phase 68–69 — for manual QA)

**Automated pre-check:** `npm test` + `npm run smoke:public:local` (HTTP routes, creator create, `vote-by-index`, JSON privacy — **not** a substitute for full browser QA).

| Step | Route / action | Auth |
|------|----------------|------|
| 1 | `npm run demo:public:local` | Seeds demo voters on `www_test` |
| 2 | `/polls/new?live=1` → share `/vote/<pollId>` | **`creator_session`** only (local-test issuer on localhost) |
| 3 | `/my-polls?live=1` | Same creator cookie — **not** user login |
| 4 | `/profile` | **MVP demo `X-User-Id`** — **production auth later**; no `creator_session` |
| 5 | `/vote/<pollId>` | `X-User-Id` + `vote-by-index`; ineligible → fixed copy (no option/index leak) |
| 6 | `/results/<pollId>` | Collecting = counter-free; revealed = display-safe aggregate |
| 7 | `/results/<pollId>?creator=1` | Creator lifecycle panel (UI not authorization) |

**Reference Answer** does **not** use profile eligibility (regression: `reference-answer-hardening` tests). Full checklist: **`docs/www-project-public-mvp-manual-qa-v1.md`** §3.10 · release readiness: **`docs/www-project-phase-69-mvp-demo-release-readiness-handoff-v1.md`** · closure index: **`docs/www-project-phase-68-public-demo-polish-manual-qa-closure-v1.md`**.

### Public MVP current status (Phase 49 — demo handoff)

**Baseline commit (Phase 48):** `630baea` — FAQ, trust-level matrix, mobile readability polish.

The public browser surface remains **share-link first**. Default routes stay **static/demo-facing** for policy UX; **creator lifecycle management** is available only with MVP dev query switches (`?live=1`, `?creator=1`) and the Phase 65A **`creator_session` cookie** (local/demo/test only — **not** production public identity). Backend route adapters for profile, Official Vote, vote-by-index, and creator-owned polls use **`UserAuthResolver`** in production mode; Phase 81 lets that resolver read a valid production `www_session`, Phase 86 adds a minimal `/login` credential-proof form for configured production verifier flows, and Phases 89–96 add, review, plan, implement, harden, polish, smoke-test, and re-review the verifier-backed registration runtime and minimal form/profile boundary. Local/demo browser flows still use **MVP demo-style `X-User-Id`** unless explicitly configured otherwise. Results follow backend `public_lifecycle_state`. There is still **no** notification persistence, trust scoring persistence, feedback persistence, or production ranking/feed personalization. Auth boundary summary: **`docs/www-project-phase-70-final-production-auth-boundary-checkpoint-v1.md`**. Production credential verifier, login/session, and registration planning/foundation/review/runtime: **`docs/www-project-phase-71-production-credential-verifier-plan-v1.md`**, **`docs/www-project-phase-72-production-credential-verifier-foundation-v1.md`**, **`docs/www-project-phase-73-production-login-session-ux-plan-v1.md`**, **`docs/www-project-phase-77-production-login-session-runtime-plan-v1.md`**, **`docs/www-project-phase-78-production-session-schema-foundation-v1.md`**, **`docs/www-project-phase-80-production-login-submit-runtime-foundation-v1.md`**, **`docs/www-project-phase-81-user-auth-resolver-session-read-integration-v1.md`**, **`docs/www-project-phase-86-minimal-production-login-form-runtime-v1.md`**, **`docs/www-project-phase-87-production-login-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-89-registration-runtime-foundation-v1.md`**, **`docs/www-project-phase-90-registration-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-92-minimal-registration-form-runtime-v1.md`**, **`docs/www-project-phase-93-registration-ui-runtime-review-hardening-v1.md`**, **`docs/www-project-phase-94-registration-login-navigation-copy-polish-v1.md`**, **`docs/www-project-phase-95-registration-login-full-flow-smoke-coverage-v1.md`**, and **`docs/www-project-phase-96-registration-runtime-profile-boundary-review-v1.md`**.

| Page | Route | Notes |
|------|-------|--------|
| Landing | `/` | Entry; links to create, explore, FAQ, trust matrix |
| FAQ | `/faq` | Static policy Q&A (Traditional Chinese); aligns with Phase 39–41 drafts |
| Trust levels | `/trust-levels` | Static Lv.0–Lv.4 permission matrix (demo copy) |
| Create poll | `/polls/new` | Default: demo preview (no DB write). **`?live=1`:** real `POST /creator/polls`, share vote link, lifecycle controls |
| My polls | `/my-polls` | Default: static mock table (inert). **`?live=1`:** live creator management from `GET /creator/polls` |
| Vote | `/vote/:pollId` | Submits `option_index` via `vote-by-index` |
| Profile | `/profile` | MVP `X-User-Id` only; `birth_year_month` + coarse `residential_region` for Official Vote eligibility (see Phase 67 QA doc) |
| Vote (demo) | `/vote/demo` | Static policy shell; optional `?ui_state=` for QA |
| Results | `/results/:pollId` | Read-only display-safe results per lifecycle; collecting stays counter-free |
| Results (creator) | `/results/:pollId?creator=1` | Creator lifecycle panel (cancel / close-reveal / unpublish) + post-transition refresh; **UI is not authorization** |
| Results (demo) | `/results/demo` | Static lifecycle shells via `?ui_state=` (see handoff doc) |
| Explore | `/explore` | **Freshness-only** list from `GET /polls/feed` (collecting, recently published); links to `/vote/:pollId`; no vote counts or ranking |

**MVP dev query params (not production UX):**

| Param | Values | Purpose |
|-------|--------|---------|
| `live` | `1` | Real create/manage on `/polls/new`, `/my-polls` using the Phase 65A creator cookie |
| `creator` | `1` | Show creator lifecycle panel on `/results/:pollId` (backend `/creator/polls/:id/cancel|close|unpublish` remains authoritative) |
| `nav` | `guest`, `logged-in-mock` | Toggle header/nav mock on static pages (not real auth) |
| `ui_state` | `collecting`, `revealed`, `locked`, `post_lock`, `cancelled`, `unpublished`, … | Preview lifecycle copy on `/vote/demo`, `/results/demo`, etc. |

**Release readiness entry (Phase 69):** **`docs/www-project-phase-69-mvp-demo-release-readiness-handoff-v1.md`**. Lifecycle manual QA: **`docs/www-project-phase-60-public-mvp-lifecycle-manual-qa-handoff-v1.md`**. Profile eligibility demo QA: **`docs/www-project-phase-67-profile-eligibility-demo-qa-v1.md`**. Integrated manual QA closure (Phase 65–68): **`docs/www-project-phase-68-public-demo-polish-manual-qa-closure-v1.md`** + **`docs/www-project-public-mvp-manual-qa-v1.md`** §3.10. Full demo URL list and product rules: **`docs/www-project-public-mvp-demo-release-handoff-v1.md`**.

**Collecting-stage privacy (product rule, MVP UI + future API):** While a poll is **collecting**, do **not** show vote counts, percentages, totals, ranking, trends, or progress — including to the **creator**. **Close** ends the voting/statistical period and reveals aggregate results; it does **not** mean the public lock period ends (MVP may use close time as reveal time). **Public lock period (MVP draft):** ~5 days after reveal — during lock, creator cannot unpublish/delete/edit/reopen/hide results; after lock, creator may unpublish. **Cancel** stops collecting (not “unpublish”); unpublish copy: 「此問卷已結束公開鎖定期，並由發起者下架。」 Ineligible users may see basic info, cannot vote, cannot see collecting results, but may **follow results** (MVP = in-app notification placeholder; email/push future). **Skip voting, view results** remains future.

**Trust levels (direction, static matrix):** Lv.0 訪客 · Lv.1 註冊用戶 · Lv.2 可信註冊用戶 · Lv.3 高信任分用戶 · Lv.4 高信任（尚未開放）. Trust level cannot bypass political/high-risk review. **功能點數** may be paid later for features/exposure but **cannot buy trust**. **信用點數** comes from quality and positive contribution and **cannot be purchased**. High-risk topics cannot bypass review by points or payment.

**Not yet implemented (Public MVP product path):** DB-backed public flow; real auth; notification persistence; trust scoring persistence; feedback persistence; production ranking/feed personalization.

Polls in `suspended` or `correction_pending` are hidden from public GET/feed/vote/result/reference-answer (backend behavior unchanged).

### `GET /polls/feed` (Phase 5B–5C)

**Privacy / ranking contract (unchanged):**

- Public, **non-personalized**, **freshness-only** — no ranking by engagement, option percentages, vote growth, Manipulation Integrity Warning, or user answer direction.
- SQL uses **`polls` only** — no dependency on `poll_options`, `poll_vote_tokens`, `poll_reference_answer_tokens`, or `poll_option_vote_counters`.
- Filter: `status = 'active'` and `published_at IS NOT NULL`.
- Order: `published_at DESC`, `id ASC`.
- Response items do **not** expose precise `published_at`, participation fields, options, or vote/token/counter data.

**Pagination (Phase 5C):**

| Query param | Rules |
|-------------|--------|
| `limit` | Optional; integer **1..50**; default **50** |
| `cursor` | Optional; opaque `v1.<base64url>` keyset cursor |
| *(other)* | `400 UNSUPPORTED_QUERY_PARAMS` |

Response includes `polls` (same safe fields as 5B) and `next_cursor` (`string` or `null`). Cursor payload encodes only **`published_at` + `poll_id`** for keyset continuation (not `user_id`, options, or vote data). Pagination uses **keyset** (`published_at` / `id`), not `OFFSET`.

PostgreSQL partial index: `idx_polls_public_feed_freshness` (`migrations/005_phase5c_public_feed_index.sql`).

## Admin correction APIs (Phase 6B–12)

Require `Authorization: Bearer <opaque-token>`. Production config supplies SHA-256 token digests, admin IDs, role, and permissions through `ADMIN_AUTH_CREDENTIALS_JSON`; legacy `X-Admin-User-Id` is not trusted. See `docs/admin-correction-http.md` and `docs/www-project-phase-14-admin-auth-deployment-v1.md`.

Full route table, DTO allowlist, status machines, public notice behavior, and stubs: **`docs/admin-correction-http.md`**.

| Method | Path | Summary |
|--------|------|---------|
| `POST` | `/admin/correction-requests` | Create correction for active/closed poll |
| `GET` | `/admin/correction-requests/:id/review-context` | Blind review context (`decision_summary` only) |
| `GET` | `/admin/correction-requests/:id/audit-record` | Safe read-only audit snapshot |
| `GET` | `/admin/polls/:pollId/correction-audit` | Paginated safe correction audit list for one poll |
| `GET` | `/admin/correction-audit` | Paginated safe global correction audit queue |
| `POST` | `/admin/correction-requests/:id/decisions` | Submit approve/reject |
| `POST` | `/admin/correction-requests/:id/apply` | Apply approved correction |
| `POST` | `/admin/suspended-correction-requests` | Suspended poll → `correction_pending` |
| `POST` | `/admin/suspended-correction-requests/:id/apply` | Apply + reactivate + write public notice |

Public correction notices are read through `GET /polls/:id/public-notices`; see `docs/admin-correction-http.md`.

## Current scope

**Implemented:**

- `users`, `polls`, `poll_options`
- Reference Answer Design B (participation token only; no durable option storage)
- Official Vote tokens and aggregate sharded counters
- Privacy-safe result display tiers
- Frontend selection state in page-local runtime memory; cleared on submit, `pagehide`, and BFCache `pageshow`
- `GET /polls/feed` — freshness-only public feed with optional cursor pagination (5C)
- Poll visibility / archive (6A)
- Real MVP public lifecycle schema foundation (54): server-controlled lifecycle fields and minimal `poll_eligibility_rules`; API behavior remains future work
- Admin typo correction foundation (6B): schema, requests, Dual-Admin decisions, apply, suspended path, admin HTTP routes (6C)
- Admin correction audit read API (7.4): `audit-record`, per-poll `correction-audit`
- Review-context decision leak fix (7.5): anonymous `decision_summary` only on workflow read
- Poll-scoped public notice read API (8): visible fixed-template notices only
- Global admin correction audit queue (9): safe cross-poll list with bounded filters
- Poll-scoped public notice display UI (11): allowlisted public notices only on `/results/:id`
- Admin Auth / RBAC v1 (12): opaque Bearer token registry + server-side permission boundary for `/admin/*`

**Not implemented (see spec and `docs/admin-correction-http.md`):**

- Full spec §32 Phase 5 Wonder Flow / ranking beyond the 5B–5C feed slice
- Feed discovery UI, filters, personalization, `total_count`
- Real Spread Score calculation, 24h pre-apply recompute, semantic typo guard
- Spread Score ranking / priority
- Full admin login/session/JWT/OAuth management and token rotation automation
- Other frontend admin UI
- Future high-sensitivity category guardrails and other deferred spec phases

Run `npm run migrate:check` for the current migration count. Run `npm test` on any branch; use `npm run test:integration:local` for the isolated local Docker `www_test`, or run `npm run test:integration` when `DATABASE_URL` points at an isolated `www_test` (see Phase 15 doc).
